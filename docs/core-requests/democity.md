# Democity integration requests — formal builder r1

These are requests, not applied cross-owner changes. Democity r1 is frozen; the literal city-density, performance and population failures remain builder failures.

1. Add ARCHITECTURE event rows `democity:staged {ms,stats}` (once after showcase setup) and `democity:tour {stop,index,total,seconds}` (every tour stop). Correct the stale spec examples to direct APIs `ctx.modules.props.place` and `transit.createLine(mode,stopIds,{name,color,vehicles,fare})`; no `.api` intermediary and no object-only line signature. The actual loop has eight stops and an implicit final-to-first leg.
2. Add aliases `industrial → industry` and `riverfront → waterfront` for whole-game camera consumers, preserving the eight democity spec preset names and every core camera. Parent requested this seam; no camera module edits were made.
3. Legacy loaded-world reconciliation: old real saves, including the organically earned404-city checkpoint, contain no `modules.democity` key because the former stub exposed no serializer. After a successful load that did not restore democity, call the public `democity.deserialize({module:'democity',version:1,staged:false,plan:null})` to clear only its outgoing owned landmarks/plan. Otherwise a saved ordinary city loaded over the demo retains demo landmarks. Never clear another owner's world data; perform this from a successful load event/result, not before validation. Current API handles this empty payload without issuing roads/zones/buildings generation.
4. An optional roads node-design-height API remains absent. Proposed `addNode(x,z,{designY?})` / `setNodeHeight(id,y)` must be owned and validated by roads, resample connected profiles, and emit roads:changed. No private node mutation or artificial grade separation was used. The current hillside highway follows real terrain; it is visibly too steep and is a layout improvement for the next democity round, not excused as inherited.
5. A public terrain regeneration/reset capability is needed for `democity.restage({seed:7})` to be identical to a new page whose terrain was generated at seed7. Restage presently restores the original terrain through its public serializer, changes democity's plan seed and re-authors via owners. It cannot legally import terrain internals or write `world.seed` / terrain features. Same-seed cached owner restore is supported; cross-seed whole-terrain equality is not claimed. A coordinated core reset should reset owner seeds/IDs and invalidate dependent caches, not add a democity-only fake feature map.
6. Optional props exclusion areas for authored landmarks could prevent automatic foliage from intersecting democity-owned civic pads. Suggested owner API takes read-only oriented rectangles with owner key and invalidates only affected chunks; clearing democity releases only its rectangles. Current democity reserves zoning land and never removes or writes props' records privately. This request does not mask insufficient lot density.

Evidence in `shots/democity/rdev1/` reports actual full-world costs and the240-step real simulation pre-roll. No added cash, forced spend, fake population, service capacity, coverage grid or alternate HUD economy is used. No request here is permission to normalize those data or relax failures. Supporting owner-level profiling and a better street/frontage plan are still needed to reach1200buildings and whole-scene budgets.

## Round 2 additions (source frozen for formal critique)

7. Add the public `ui.dismissTransientNotifications()` operation prepared by the root integrator. It clears expiring milestone banners and non-sticky toasts while preserving journal, real reward history, permanent warnings and native economy. Democity calls it optionally after deterministic staging. Until integrated, the required r2 capture matrix uses twelve measured seconds so existing notification timers expire normally. No HUD internals are mutated.
8. The r2 bootstrap deliberately runs the initial native settlement before the final paid civic expansion, then creates the final bus line and steps one further simulated hour. This is a disclosed ordering adaptation: the original three-person town could not fund all facilities before sufficient tax base existed. Exactly120,340 public native ticks produce8,041 real residents in the tested seed1337 plan. Tax12%, retained public loan principal136,000, final cash25,865 and current native daily net+12,028 are genuine. The final transit operating-cost integration is still pending; no bus expense is secretly offset with added funds. These are deterministic pre-simulation results, distinct from the root's400+ population ordinary user-interface checkpoint.
9. R2 uses existing public `props.setDensity`, `props.remove` and `props.place` for woodland density and physical forecourt/park planting. The optional rectangle exclusion request6 is no longer necessary to remove current owned landmark/tree intersections; records remain owned, serialized and validated by props. No duplicate democity props mesh or fleet was added. Public props placement has a real cost due to patch rebuilding, included in stage timing.

Remaining scale, sparse neighbourhood enclosure, native footprint seating and composed GPU/memory failures still belong in the builder report. No cross-owner exception is claimed without a reproduced owner-showcase comparison; the integration requests do not waive acceptance thresholds.

10. **Confirmed round2 all-mode startup defect, discovered after source freeze.** `buildings/index.js:399` automatically spawns the first400 lots only when `flags.showcase==='democity'`; `all` correctly stages the democity module but preserves its original flag. R2's early settlement therefore has no initial stock in all mode, unlike its democity URL. Root's independent all-mode PNG has population1/cash429/net−10,554. This is not an equivalence pass. Minimal additive integration fix, directly before the high-density level initialization in democity: `if (ctx.world.buildings.items.size < 400) ctx.modules.buildings?.spawnFreeLots?.(400 - ctx.world.buildings.items.size);`. The existing public method iterates native lots in exactly the observer order, flushes and emits; count is additional buildings, so calling it unconditionally with400 would be wrong. Keep frozen formal evidence unchanged, apply after the round and independently retest both URL modes and identical native initial stock. No flags or economic state should be assigned to conceal the difference.

## Round 3 checkpoint (current cycle only; no round 4 authorized)

11. Request10's all-mode stock fix is now implemented in the owned module before deterministic settlement growth. It fills only the shortfall to400 through public `buildings.spawnFreeLots`; no economy, flags, population or reward records are assigned. Fresh all-mode smoke has8,041 residents before the subsequent civic expansion; final mode-equivalence evidence is `shots/democity/rdev3/aliases.json` and must be read for the final city's exact result.
12. Final civic expansion requests six schools and three clinics with distinct neighbourhood targets, in addition to the two hospitals and existing high school. All placements use native validation and are paid. The final census is302/511 residential centres with both health and education coverage (59.10%, still below60%), with30 facilities. Native stock falls652→611 because these real facilities reserve formerly zonable land; this regression is explicit rather than concealed by fake lots. Native population8,047, cash25,848.00 and net+10,247.50/day retain a231,000 principal loan before the root's pending transit fare/expense adapter.
13. Public props startup nuance: `setDensity` rebuilds only an already-built owner. Fresh demo staging reaches props before its deferred first build; omitting rebuild leaves landscaping APIs unable to place/remove. R3 calls `setDensity(.65)` then `rebuild()` only when public `count()` is zero. Real forecourt/landmark tree removal and park planting then occur via owner APIs. No private built flag is read. Near-night visibility improves but the frame still addresses an open lawn/building wall rather than a convincing street canyon.
14. The road layout is preserved at this checkpoint. Four actual-browser public-layout trials without split midpoint nodes yielded741/823/805/790 buildings for80×80/96×96/96×80/80×96 m blocks, but all lost the400-node requirement and some also lost450 edges. They were not adopted. These are recorded investigations, not final scale passes. Remaining native lot yield, bridge clearance, seating, lighting, district grain, seed lifecycle and whole-scene budget failures need future authorized work. No new development round is started after this checkpoint.

## Integrator decision (wave 3 final, 2026-09-08)

Applied requested camera aliases, legacy-load landmark cleanup and public transient-notification cleanup; documented democity:staged/tour events and actual public API signatures. Retained owner r3 shortfall-to400 startup correction and public paid civic/props staging. Deferred coordinated seed/terrain reset and roads design-height authoring because they require multi-owner state/geometry work beyond this cycle. Optional landmark rectangle exclusion is unnecessary for current owner workaround using public props placement/removal. No density, coverage or budget failure waived. See `docs/critic/integration_w3.md` for final checks and open issues. The user requested a checkpoint and pause; no new round is authorized.
