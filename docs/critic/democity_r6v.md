# Democity r6v local critical review

**Verdict: 6.0 / 10 — FAIL (score held).**

r6u establishes that the real moving-camera planar reflection is a measured cost, while disabling it entirely still does not reach 50 fps. r6v reduces only the High target's pixel count. Exact A/B evidence makes the visual tradeoff reviewable, and the observed whole-image differences are below 0.04%. Geometry, scene content, camera immediacy, APIs and deterministic restage are unchanged.

The gain is only about 0.5–1.2 fps under continuous camera motion. The standard matrix's minimum remains variable and failed, so it would be wrong to call the 50 fps gate closed or raise the score. The existing city-scale, memory, lighting, seating, grain, activity and lifecycle failures are unaffected.

Accept r6v and retain 6.0 FAIL. Profile the remaining non-reflection owner cost instead of reducing terrain detail or water quality further.
