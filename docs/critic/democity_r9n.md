# Democity R9n — independent coordinated-infill screen

**ACCEPT the qualified read-only screen and advance only `1,0:v` to disposable actual-owner staging. This does not accept a product change. Democity and whole-game remain6.0/10, FAIL against8.5; rank1 stays open.**

I read the complete767,918-byte JSON, both relevant prior critiques (R8y/R9j), the latest revised builder report, the profiler, current authored plan and public road-tool evaluator/snap/commit contracts. No screenshot or browser rerun was required for this noncommitted numerical screen.

## Recomputed retained results

| Result | seed1337 | seed7 |
|---|---:|---:|
| Reported authored grid nodes |268|280|
| Reported candidates after endpoint-edge filter |281|318|
| Retained/tested drafts |100|100|
| Publicly valid drafts |89|82|
| Core0 / endpoint≤3 proxy, before validity |20|28|
| Same proxy and publicly valid |19|17|
| Edge-to-edge snapped drafts |53|48|
| Nodes / edges, before=after |468 /604|493 /646|
| Lots / buildings / painted cells, before=after |612 /612 /9,964|648 /648 /10,113|

The complete grid, world maps and lower-ranked candidate rows are absent. Therefore281/318 and268/280 are reported tool counts, not totals independently rebuilt from retained primitives. For all200 retained candidates I independently verified key uniqueness, proposed length, rank formula, aggregate bounds, first-click success/cost0, snapped endpoint coordinates, two-point draft length and rounded grade. The retained ordering exactly matches the declared comparator, including locale tie-breaking. This verifies ordering within the saved100, not that no omitted row should outrank them.

The valid arrays exactly equal the strict boolean-valid subsets. All29 invalid drafts exceed12% mean endpoint grade and report a grade failure; all171 valid rows have `reason:null`. All engine/browser/HTTP error lists are empty. Both five-count before/after objects are equal. The script's aggregate `pass:true` recomputes exactly; it requires two error-free unchanged-count seeds and at least one common valid row, not a full product or selected-candidate integrity gate.

I rebuilt both valid-key maps, their41-key intersection, every cross-seed minimum/maximum and the common sorting. Exactly three common rows satisfy core0 and endpoint≤3:

| Key | Minimum unclaimed band | Maximum core / endpoint | Maximum displayed grade |
|---|---:|---:|---:|
|`-3,1:h`|17|0 /3|5.78%|
|`1,0:v`|17|0 /3|3.83%|
|`0,1:h`|4|0 /2|2.05%|

These are **proxy-safe**, not proved harmless or productive. The selected row is12th/23rd in the respective seed rankings and second in the three-row list. Selecting it over the96m outer alternative is a reasonable bounded prioritization based on central location, shorter length and lower mean grade. It is not a statistically or geometrically proved optimum. All three shortlisted routes happen to retain the same x/z endpoints across these two seeds, so that property is not unique to `1,0:v`.

## Selected candidate and snap contract

The selected unwarped line is exactly `(80,40)→(80,120)`, length80m. Its first/second snaps are real edges613/656 on seed1337 and661/707 on seed7; their prefilter distances are zero and actual snap coordinates/IDs match. Both drafts are directly `valid:true`, cost96. Endpoint terrain rises are2.244964600m and3.064496994m, yielding2.806205750% and3.830621243%, correctly displayed as2.81% and3.83%. At the current alley rate1.2/m, both under4% surcharge threshold, the stated price96 agrees.

The screen retains21/17 painted unclaimed side-band cells, no claimed core cells, three claimed endpoint-neighborhood cells and zero of21 water samples. Maximum sampled terrain slope is0.183592574/0.376732439. In particular, lower displayed grade does not mean flatter intervening terrain or lower cut/fill: the alternative's seed7 maximum sampled slope is0.073784101. Public evaluator validity includes its own terrain/cut/water checks; it does not predict frontage yield.

Across all400 retained endpoints, snapping preserves proposed x/z coordinates. Every edge snap matches the prefilter edge ID. However, the pairs include39/42 node-to-node,6/8 edge-to-node and2/2 node-to-edge drafts. Only53/48 are edge-to-edge. The graph already has midpoint nodes on many streets. Thus the broad candidate set cannot all be described as requiring two edge splits. The selected row does require that path if committed as recorded.

The prefilter only requires four authored corner nodes and two nearby distinct straight endpoint edges. It does not establish four closed block boundaries, a vacant interior, all road crossings or protection from service/landmark footprints. Magnet snapping prioritizes nodes within12m before edge snapping within10m; the diagnostic prefilter radius is8m. Its chosen edges need not universally be the ones ultimately used, although every retained edge snap matches here. The revised builder correctly narrows these claims.

## Source scope and no-mutation boundary

The authored plan marker occurs once. Reconstructing its sole routed replacement reproduces the recorded hash: shallow copies of existing grid records are exposed to a diagnostic global, followed by the original return. The current product plan remains exact. The screen loops synchronously in fresh paused worlds, calls query functions, selects a road tool, sets its first anchor, previews its endpoint and cancels. `roadTool.click` adds a draft point; it does not call commit. Splits, road rebuilding, spend and undo recording occur only in the uncalled commit path.

This supports no intended world-road/terrain/zoning/economy mutation from the tested path. The diagnostic global, tool selection, previews and query caches are runtime changes. Equal counts do not independently establish identical node/edge payloads, terrain heights, finance, services, simulation or saved state; those serializations are not retained. The latest builder discloses that evidence boundary. No public commit, physical road split or gameplay transaction has yet been exercised.

Verified hashes:

- JSON: `6c958ce19cf834414135fc1d6701062a7a8ffef696bc170a84773565db9a8271`.
- Screen tool: `b1641658f6e9296aa63d50a19365fafaa6a8419610b698de18af3f1d53e42460`.
- Product plan: `c439a6e88f87f40aac5182698d4406540e21f04ce659975b2b0e8cb4e4d26bd6`.
- Reconstructed routed plan: `bb95e06de52aededb99989e04cb747626bdf52a3bf5377f29d53f82d06e78d75`.

The companion JSON records current Tools/Roads source hashes as well. Transformed fetched source bytes and runtime renderer identity are not retained. Metal/Chrome and1280×720 are launch settings here, not independently verified renderer telemetry.

## Limits, judgment and next test

The rank is `4×unclaimed −8×claimedCore −2×claimedEndpoint −20×waterSamples`; maximum sampled slope is recorded but does not affect it. Side-band cells are current painted cell centers7–34m from the line within8–92% of its length. The10m core and24m endpoint checks are contact proxies, with different longitudinal limits, not actual road footprints or claims about lost buildings. They ignore unpainted future opportunities and do not emit lots. Raw per-cell keys and terrain samples are absent, so their geometric membership cannot be independently reconstructed from this JSON alone.

Independently truncated top100 lists mean41 is not all valid common designs. R8y/R9j exhausted only the two common positive existing-node/cardinal shortlist entries; they did not exhaust mid-block links or coordinated designs. This is a legitimate new diagnostic direction, with no demonstrated density or visual benefit yet. No screenshot, performance, API/save or score gate follows from it.

**Next: commit only `1,0:v` through public Tools in disposable worlds for both seeds.** Verify actual split journals/topology and references, terrain change, payment/undo/redo, regenerated lots/buildings and claimed/painted cells, retained identities, service/transit effects and saved-state restoration. Repeat staging deterministically and inspect matched directed images. Two interior straight-edge splits would ordinarily yield+2nodes and net+3edges (replace2 by4, then add1connector), but this is an expectation to verify, not an observed result. Reject product adoption if cross-seed useful stock, integrity or visual coherence fails. Keep6.0 FAIL until actual quality evidence warrants otherwise.
