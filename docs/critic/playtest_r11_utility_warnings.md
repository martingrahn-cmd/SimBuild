# Local critical review — PT-20 presentation

**Decision: accept bounded UI fix; scores unchanged.**

The prior detail-only `No power` state was too easy to miss and the absence of cables/pipes looked like an omitted system. The accepted UI reads only Simulation's public per-building values and does not manufacture shortages. The directed fixture visibly distinguishes no-power and no-water buildings; the service guide states the existing road-distribution and paid-import contract. Both images are legible without obscuring the selected buildings, and the nearest-48 cap avoids unbounded DOM cost. Contract and regression probes report zero errors.

This closes the recorded presentation gap locally. Human retest is required for marker prominence in an ordinary growing city. No service or UI critic score is changed.
