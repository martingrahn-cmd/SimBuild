# New Dollarton human-playtest checkpoint — R16, 2026-09-12

**Current player-facing title: New Dollarton.** `SimBuild` remains the repository name, hosted path, cloud-save game ID and internal compatibility prefix; changing those identifiers would break existing links or saves.

The main and pause menus display `Version 0.1.0 · build <7-character source commit>`. The GameVolt sync action injects the source commit into the compiled bundle, so the label identifies the code actually running even while an older Cloudflare-cached page is being served.

R16 makes the existing GitHub **Report a bug** action clearly visible in both the main and pause menus. It has a distinct warm accent, a 54px target and the explanation **Help improve the game**. The automated probe confirms the unchanged Issue Form destination and both inspected captures are zero-error. See [R16 build evidence](builds/playtest_r16_bug_report_visibility.md) and [local review](critic/playtest_r16_local.md). UI remains **7.0 FAIL** and whole-game remains **6.0 FAIL**; this is a discoverability acceptance, not a rerating. Product commit `ad645dd` is published in GameVolt source `d669f33`; sync run `34715641937` and Pages run `34715661435` passed.

R12 accepts a bounded MVP repair without changing scores. The phantom parked row was the traffic showcase catalogue leaking into normal play and is now showcase-only. A 22-resident residential fixture now produces 7 passenger vehicles instead of 17 mixed vehicles; empty roads remain zero, mixed land use produces deterministic commute/customer/freight purposes, dead-end U-turns remain valid, and Traffic restores exactly. Settlement below 150 residents is faster (28 after 0.25 day, 97 after one day in the fixed fixture) while the established-city rate is unchanged. Settings now includes persistent **Always daylight**, and main/pause menus include **Report a bug** linked to the new GitHub Issue Form. See [R12 build evidence](builds/playtest_r12_mvp_repair.md) and [local review](critic/playtest_r12_local.md). Manual service placement rotation is also implemented with **R** in 45-degree steps. Human replay of traffic density and population feel is still required; scores remain whole-game 6.0 FAIL, Traffic 7.2 FAIL, Simulation 6.5 FAIL and UI 7.0 FAIL. The R12 product commit is `77bfd55`; GameVolt sync run `34695172075` and Pages run `34695195397` passed, and the live version endpoint reports source `77bfd55`.

## Current boundary — R12 ready for human retest

The bounded R10 response to the 2026-09-10 playtest is implemented and verified. Use `http://127.0.0.1:5180/?mode=play`; port 5173 currently belongs to an unrelated project and is not the SimBuild server. A new city gives the clearest test of progression, imports and construction, although existing saves remain supported.

Accepted local R10 changes are: demand-backed traffic spawning; reverse-edge U-turns at ordinary two-way dead ends; restrained distant vehicle lights and longer full-geometry range; an explicit Select / Inspect tool; corrected road and zoning guidance; paid visible starting utility imports with Hamlet utility tools; deterministic saveable building construction; faster early move-in/growth; render-time tree wind; quieter/later crickets; and an immediate event-driven loading view. Full details and evidence are in [human_playtest_r10.md](builds/human_playtest_r10.md) and [the updated playtest ledger](playtests/human-playtest-2026-09-10.md).

Verification passes: production build (Vite 8.2.2, 163 modules), 16-module normal startup with zero errors, gameplay entry, IndexedDB save/load/delete and camera/time restore, traffic causality/U-turn/portal contracts, independent utility replacement, construction completion/save-load, 1×/4×/paused wind timing, day/night audio mix and real staged loading progress. Props remains the one semantically non-exact owner in the general checkpoint probe. The test server is separate from the build verifier and remains on port 5180.

Critic scores are deliberately unchanged: whole game **6.0/10 FAIL**, blind A/B **0–4 versus CS2**, and no module reaches 8.5. R10 has no formal critic rerating. Existing performance, memory, city-scale, Props, transit and visual gates remain failed where recorded in `STATUS.json`.

Human retest should concentrate on the opening pace, the length/readability of construction, traffic emerging only after activity, U-turn presentation, distant night vehicles, tree motion, cricket volume and whether the revised road/zoning/utility guidance explains the first build loop. New finding **PT-2026-09-11-14** records that the daylight river reads as a terrain hole filled with the sky texture rather than flowing inland water; its reflection, depth/absorption, surface motion and bank transition must be attributed separately using `shots/playtest-checkpoint-2026-09-11/human-river-sky-hole.png`. New critical finding **PT-2026-09-11-15** records that at 18:34 in winter the terrain, shore and unlit road are too dark for reliable building; evidence is `shots/playtest-checkpoint-2026-09-11/human-night-too-dark.png`. New usability requirement **PT-2026-09-11-16** requires an in-world, rule-driven frontage-depth marker so road/zoning tools show exactly how far houses may grow and which cells are blocked. Source inspection confirms **PT-2026-09-11-17**: the locked Roundabout card sets `junction: roundabout`, but no placement path consumes that option, so no usable prefab roundabout exists. Human evidence **PT-2026-09-11-18** shows that R10 overcorrects traffic causality: a connected city with about 22–26 residents and real buildings has almost no visible vehicles. Preserve the exact-zero empty-road contract, but restore a small visible activity-driven fleet for real residents/jobs. **PT-2026-09-11-19 is fixed and verified:** Tools now yields Escape to UI while already neutral, so one Escape exits an active tool and the next opens Pause; the gear also opens Pause rather than Settings. Both routes expose Save Game/Load Game, and the rebuilt 163-module production artifact plus updated `ux-utilities` probe pass. The next autonomous step is PT-18, then PT-15, PT-16 and PT-17 before diagnosing PT-14 and continuing the queue.

The recoverable R10 source checkpoint is `/Volumes/ExtDrive/SimBuild-verification-2026-09-06/checkpoints/human-playtest-r10-2026-09-11` with SHA256 `c151d33333d864666dd41c089db310df926654c6869a17f3254131ea253ea430`. Final `STATUS.json` and `HANDOFF.md` are copied beside the archive.

## Historical stop boundary — player checkpoint frozen at accepted R9u

The 2026-09-10 human playtest is now stopped for the night with **13 findings and proposals** saved in [the consolidated playtest report](playtests/human-playtest-2026-09-10.md). No gameplay source changed during the session. Resume only on a new explicit instruction.

A later product proposal, **PT-2026-09-11-13**, records GitHub source/build automation with primary deployment to the owner’s game portal; GitHub Pages is optional. It also records an in-game GitHub Issue Form or protected portal API diagnostics handoff. The existing remote is still at the 2026-09-06 commit; do not push the 5.2GB local evidence corpus or embed repository credentials in the client.

Autonomous development is paused by user request; the completed human-feedback addendum is authoritative for the next work order. The player-facing source is the independently accepted R9u checkpoint at unchanged **Democity 6.0/10 FAIL** and **whole-game 6.0/10 FAIL**. R9v never reached critic review or integration: its mixed Building/Service/Road marquee candidate passed the injected service-refusal rollback but failed complete-owner verification because Props changed **5,972 → 6,013 → 6,021** across commit/undo. The R9v delta and evidence are preserved in [R9V-UNVERIFIED-CANDIDATE.md](history/R9V-UNVERIFIED-CANDIDATE.md), and the player source was restored byte-for-byte from the frozen R9u/R9t evidence copies.

The normal entry point is `http://127.0.0.1:5173/?mode=play`. A clean Vite build passes all **163 modules**. Fresh startup reaches the main menu with all **16 runtime modules ready and zero errors**; Continue enters the empty player map with the toolbar visible. A temporary IndexedDB slot saves, loads and deletes successfully, and restores the complete stable gameplay-owner payload plus camera. Props serialization is not byte-identical after the round trip and the restored hour differs by about `2e-15` hours; both are disclosed in `shots/playtest-checkpoint-2026-09-10/verification.json`. This checkpoint does not claim exact Props save replay.

Current scores are: terrain 6.0, environment 7.0, roads 6.0, simulation 6.5, UI 7.0, audio unscored, effects 6.0, zoning 8.0, buildings 7.4, props 6.4, traffic 7.2, tools 8.0, democity 6.0, services 7.0, infoviews 6.4 and transit 6.8. No module meets the 8.5 pass threshold. Blind r2 remains **SimBuild 0–4 CS2** with high confidence.

Current performance evidence remains failed where stated. The latest 16-frame matrix records **468 maximum draws / 2,275,778 maximum triangles / 49.5fps minimum**, with one of 16 frames below 50fps and **730.2MB maximum raw heap**. The whole-game r2 baseline records **473 draws / 2,807,186 triangles / 46.2fps minimum**, with 14 of 32 samples below 50fps. A later single smoke reaches 55.8fps / 430 draws / 2,061,474 triangles / 489.6MB, but one frame is not sustained evidence. The historical isolated aerial-night building attribution is 299 draws / 562,080 triangles and is not a whole-scene baseline.

Remaining issues, in checkpoint priority order:

1. **New human-playtest critical finding PT-2026-09-10-01:** dense traffic appeared immediately on a newly committed isolated road in undeveloped terrain, with no buildings or visible outside-network connection. This visibly breaks demand/route causality. Evidence: `shots/playtest-checkpoint-2026-09-10/human-isolated-road-instant-traffic.png` (SHA256 `f95c3cdede5c6abe132b4fc4184463fe8ec648ec23ebe05e168e33e2f4d0e81d`). Diagnosis and any fix are deferred while development is paused.
2. **New human-playtest critical finding PT-2026-09-10-05:** vehicles disappear when they reach an ordinary dead-end node. Despawn is valid only at explicit outside connections; a local dead end needs a destination or deterministic turn-around/U-turn. Evidence: `shots/playtest-checkpoint-2026-09-10/human-dead-end-vehicles-despawn.png` (SHA256 `4fada7e75b565c7239404b93c5d9201edb40873eae321375f39ed6410b2091dc`).
3. **New human-playtest critical finding PT-2026-09-10-08:** source inspection confirms an undisclosed bootstrap: while `servicesManaged` is false, new cities receive full power/water/garbage coverage with no visible source, capacity or cost; power/water construction unlocks at 150 population (Tiny Village). Replace this with an explicit, balanced starting utility contract and verify progression, finance and save/load.
4. **Human-playtest startup addendum PT-2026-09-11-12:** initial loading feels too long. Measure cold start to visible shell/menu and interactive game; render menus early and drive a loading view from actual asset/module/world stages with current-stage and error feedback, never a decorative looping percentage.
5. **New human-playtest high finding PT-2026-09-10-06:** no clearly visible neutral pointer/inspect mode. Escape already enters the internal null-tool selection path, but the toolbar needs an explicit cursor control and selected-state feedback, with unambiguous pause-menu behavior.
6. **New human-playtest high finding PT-2026-09-10-09:** initial city growth feels too slow. Measure normal-play time to first homes, occupancy and the first milestone; coordinate with PT-02 so visible construction provides feedback without delaying the opening, including concurrent early construction where supported. Screenshot evidence shows Day 4 / April 2031 at population 21, still Hamlet, despite a substantial road layout and many completed homes: `shots/playtest-checkpoint-2026-09-10/human-slow-opening-day4-pop21.png` (SHA256 `ca86980595a9b8e5aa416c3c9ddd599ef1dad20f7a2b488677351ede456c9748`).
7. **New human-playtest high finding PT-2026-09-10-02:** zoned buildings appear immediately as completed structures. Add a visible construction phase whose progress belongs to Simulation/Buildings and survives save/load; design, implementation and verification are deferred while development is paused.
8. **New human-playtest high finding PT-2026-09-10-03:** trees shake/vibrate instead of swaying. Reproduce at pause, 1× and maximum speed; verify render-time versus simulation-time ownership plus wind frequency/amplitude before changing foliage animation.
9. **New human-playtest high finding PT-2026-09-10-04:** distant nighttime vehicles lose readable silhouettes and merge into stretched bright clusters. Attribute vehicle LOD geometry/scale, headlights and bloom independently at matched camera distances. Evidence: `shots/playtest-checkpoint-2026-09-10/human-distance-vehicle-distortion.png` (SHA256 `849fd223ba0655188a0611547914f349ab1116808dad66d60c6106419b646171`).
10. **New human-playtest high finding PT-2026-09-10-07:** birdsong is acceptable, but cricket ambience is irritating in the normal mix. Lower its prominence and repetition, add event variation, and gate it by appropriate time, season and weather before audio is scored.
11. Props same-topology regeneration is not count/byte stable in the rejected R9v path, so mixed marquee demolition remains untrusted.
12. Ordinary pacing, clarity of choices and feedback require this human playtest.
13. Sustained 50fps and 512MB gates still fail.
14. City fabric remains sparse at 612 lots/buildings and 4,882 of 9,964 painted cells claimed.
15. Transit/traffic ownership, bus art, shadows, obstruction and the absent tram remain open.
16. Save/load works, but exact Props serialization replay is not established.
17. Foliage distance coherence, ground contact/grades, night depth, weather/horizon art and street activity remain below the critic bar.

During playtesting, observe whether the first road → zoning → service sequence is understandable; whether previews, rejection reasons, costs and refunds match the result; whether growth and economy feedback are legible at 1x and 3x; whether camera, selection, overlays, minimap and panels support decisions; and whether a manual save/reload preserves continued editing. If bulldoze/undo is exercised, note any roadside props that change. Mixed marquee demolition is explicitly outside the trusted R9u scope.

After a new user instruction to resume autonomous work: verify **PT-01/PT-05** traffic causality; replace **PT-08** magical utilities; measure **PT-12** cold-start shell/menu and interactive timing and implement an honest staged loading view; then continue through **PT-06/PT-10/PT-11** controls/onboarding, **PT-02/PT-09** construction/pacing, **PT-03/PT-04/PT-07** visual/audio findings, and finally the seed-7 Props diagnostic. Apply established evidence and critic gates. The checkpoint artifact is `/Volumes/ExtDrive/SimBuild-verification-2026-09-06/checkpoints/human-playtest-r9u-2026-09-10`.

## Prior continuation record — R9u accepted, R9v was active

R9u is independently accepted for bounded single straight-road demolition history at unchanged **6.0/10 FAIL**. Exact retained owner hashes and money pass for a real street and avenue, including later owner state/construction and tested one-shot Zoning/rebuild refusals. The integrity helper does not prove full reverse uniqueness, and grouped/marquee, curved, persistent-failure, native-input and long-play behavior remain open. See [builder evidence](builds/democity_r9u_road_demolition_history.md) and [independent review](critic/democity_r9u.md).

This instruction is superseded by the stop boundary above. R9v was locally rejected before critic review and must not resume until the Props divergence is understood.

**W5 r8g through r8n, R8p, R8q and R8v are independently accepted only as local improvements at unchanged 6.0 FAIL. r8g extends real mixed-use frontage 14→32 but leaves only 4,882/9,964 painted cells claimed and 612 lots/buildings. r8h keeps 72 LOD1 cards/168 triangles and redistributes 24 into the crown core. r8i adds deterministic cabinet/partition cues to existing lit-window variants. r8j/r8k add static hardstand and silo detail without establishing port operation. r8l/r8m align the night and low-sun water/fog display, and r8n removes the reproduced sunset NaN/black scene. R8o rejects and fully reverts four mountain-material candidates. R8p removes the Props signal-query callback hotspot by 96.0–98.2%; R8q removes hidden Traffic fallback-mast frame work by 55.3–56.8%. Neither certifies general FPS or memory. R8r independently supports a bounded LOD1 experiment; R8s rejects that Lambert candidate and restores all three files exactly after unstable −0.26%/+1.62% medians. R8t/R8u establish the real parcel and slot evidence. R8v normalizes7/8 redundant lot-cell references while preserving last ownership, IDs, buildings and economy; it creates no capacity. R8w accepts the exact raw slot-key diagnosis only. R8x independently rejects all four tested edge-order variants. R8y accepts only a qualified no-candidate screen. R8z independently rejects its disposable LOD1 branch whorl because the small exposed-trunk gain does not repair crown or impostor coherence and costs 72 triangles per LOD1 tree. R9a independently accepts a bounded Props range change from 175 to 205 m, replacing the nearest planar impostors with accepted LOD1 crowns while retaining the 6.0 score and failed FPS/memory gates. R9b accepts a qualified unchanged-source transition diagnosis. R9c independently accepts the repaired synchronized ledger, closes the prior forced-cap census mismatch and confirms that the ordinary endpoint difference remains inside the existing six-metre update threshold; it makes no product change and keeps 6.0 FAIL. R9d confirms a structural LOD1/impostor mismatch across angles and rejects alpha0.50 as insufficient. R9e independently accepts the bounded broad/narrow side-atlas improvement at unchanged 6.0 FAIL; the daylight selector-band mismatch falls 14.00–16.35%. R9f independently closes the measured142-pixel top-cell bleed as verification-only: exact isolated controls show only sparse rendered edge flecks and no visible defect justifies a product containment edit. R9g independently rejects the actual16m street at -8,-11→-7,-11 after -1/0 net lots on seeds1337/7. R9h independently accepts the bounded fresh-start allocation trace: only1/2 existing frontage rows change, while sequential fresh IDs explain the wider building-form drift. R9i independently accepts rejection of the same-pair real alley despite +1/+2 net lots because the public player tool marks the exact20.80%/12.32% drafts invalid against its12% grade limit. R9j independently rejects the other common positive R8y link: it passes the public tool at9.73%/7.36% but yields0/+1 net lots and loses23/19 painted cells. R9k independently accepts a bounded whole-owner diagnosis: Props has the strongest stable local headless-on response, but no product/performance gate or score changes. R9l independently accepts bounded Props component attribution and the verifier exit repair; all aggregate runs stay failed and no product/score changes. R9m independently accepts selector/color/shadow attribution without a product edit; ordinary geometry splits exactly and top-down timing remains open. R9n coordinated mid-block frontage screening is active. Fresh whole-game r2 stays 6.0 FAIL and blind r2 SimBuild0–4 CS2; Transit remains6.8 FAIL.** Read `STATUS.json.executionControl` before continuing.

## Verified W4 review state

- Democity r4 adds one real, paid school through the existing frontage and coverage APIs. Fresh W4 evidence re-review holds the score at **6.0 FAIL**: all 37 new captures are zero-error and the fresh public probe passes API/serialize/tour checks, but night lamps, golden-hour washout, 5.08M matrix triangles and ~1.0GB heap remain blockers. See [builder evidence](builds/democity_r4.md), [local critic report](critic/democity_r4.md), and [fresh review](critic/democity_r4_independent.md).
- Transit r4 darkens only bus paint at night. Fresh W4 evidence re-review holds the score at **6.8 FAIL**: 26 new captures are zero-error; 3 lines, 24 stops, 12 buses and public save/controls pass. The bus remains boxy, the night-stop camera lacks a docked bus, and secondary shadow flags remain unresolved. See [builder evidence](builds/transit_r4.md), [local critic report](critic/transit_r4.md), and [fresh review](critic/transit_r4_independent.md).
- Democity r5 narrows lamp pools, lowers their mesh/transparent cost, and adjusts the existing exposure curve. All standard and seed-7 captures are zero-error; matrix peak falls from 5.08M to **4.47M triangles** and the minimum frame rate rises from 27 to **35.6 fps**. It still fails the 3M / 50fps and heap gates, and the 17:30 skyline remains washed out, so the local score holds at **6.0 FAIL**. See [builder evidence](builds/democity_r5.md) and [local review](critic/democity_r5.md).
- Democity r5d limits near-grass scatter, ends furniture shadow casting beyond 360 m, and applies a low-sun exposure compensation only when the camera faces the sun. Its 16-frame first-pass matrix is zero-error but conservatively peaks at **4.59M triangles / 37 fps**; repeat controls settle lower, exposing a capture-settling discrepancy that remains open. The sunward 17:30 crop is measurably darker but still washed out, and the probe heap remains 1009.4 MB primary / 1264.6 MB after seed-7 restage. Score remains **6.0 FAIL**. See [builder evidence](builds/democity_r5d.md) and [local review](critic/democity_r5d.md).
- Democity r5e corrects the verifier rather than game code: it reports the peak over every rendered frame, including water's eighth-frame planar reflection. Three aerial-night controls agree exactly and the all-zero-error 16-frame matrix records **4.63M triangles / 960 draws / 32 fps**. This is the valid conservative performance baseline; score remains **6.0 FAIL**. See [verification record](builds/democity_r5e.md).
- Democity r5f profiles that reflection before changing source. Props account for 40 draws / 853,376 triangles of the 148-draw / 1.466M-triangle reflection pass. Props now self-cull only during that low-resolution pass; buildings, roads and terrain proxies remain. The zero-error 16-frame peak falls to **3.94M triangles / 915 draws / 31.3 fps**, but the score remains **6.0 FAIL** because the 3M/50fps/512MB gates and visual/city-scale issues remain. See [build record](builds/democity_r5f.md) and [local review](critic/democity_r5f.md).
- Democity r5g replaces object-per-triangle pavement query storage with packed data. Its allocation sample falls from 20.25MB to 1.08MB in `surface.add`, 1,095/1,095 real road queries remain valid, and the clean 16-frame matrix retains r5f's **3.94M triangles / 915 draws** peak. Total heap is still above gate and variable (fresh public restage 1,105.7MB), so the score remains **6.0 FAIL**. See [build record](builds/democity_r5g.md) and [local review](critic/democity_r5g.md).
- Democity r5h tested a one-entry owner-snapshot cache to reduce retained heap. It was **rejected and reverted**: 1337→7→1337→7 changed authored IDs when an evicted seed was rebuilt. The restored production cache passes the same cycle exactly. See [rejection record](builds/democity_r5h_rejected.md); do not repeat this cap without an identity-preserving restore design.
- The r5h main-pass profile attributes 1.169M peak triangles to roads, 895k to props, 861k to services and 562k to buildings. Props-only LOD experiments cannot safely deliver the full remaining budget. Investigate owner-safe services distance geometry next; do not hide paid facilities or alter their coverage semantics from a profiling result.
- Democity r5i implements that services geometry investigation. Detail meshes are retained inside 800 m and outside use a proxy derived from the same real facility IDs, footprints, positions and headings. The 480 m variant was visibly monolithic in aerial noon and was rejected. The accepted 800 m matrix is 16/16 zero-error at 812 draws / 3,909,080 triangles / 29.2 fps; public API/double-deserialize/tour and exact 1337→7→1337→7 authored-restage pass. Score stays 6.0 FAIL. See [builder evidence](builds/democity_r5i.md) and [local review](critic/democity_r5i.md).
- Democity r5j attributes roads' 1.169M triangles chiefly to concrete (988k). It keeps the full authoritative road accumulator and PavementSurface, but beyond 800 m renders only concrete top faces, omitting distant curb/parapet/deck sidewalls. The matrix is 16/16 zero-error at 801 draws / 3,909,080 triangles / 34.5 fps; API/double-deserialize/tour and exact authored-restage pass. Score stays 6.0 FAIL. See [builder evidence](builds/democity_r5j.md) and [local review](critic/democity_r5j.md).
- Props r5k tested automatic top-down tree impostors beyond 360 m. Though aerial noon fell to 2,632,860 triangles, the inspected city read as flat circular sprites. It is rejected and reverted; no score or performance claim. See [rejection record](builds/democity_r5k_rejected.md).
- r5l's clean temporary-mask attribution finds buildings at 299 draws / 562,080 triangles in aerial night. Buildings already own 128 m chunks and deterministic LOD0/LOD1 geometry; inspect that contract before changing it. Raw evidence: `shots/democity/r5l/buildings-profile.json`.
- Buildings r5m tested exposing the shadow-only LOD2 in the colour pass. At the useful 560 m threshold, it made large grey untextured silhouettes; at 720 m it saved too little. It is rejected and reverted. See [rejection record](builds/democity_r5m_rejected.md).
- Terrain/effects r5n attribution finds terrain responsible for 695,017 triangles / 117 draws in the same aerial-night pass and effects with no own geometry. Existing terrain LOD-scale trials at 0.7 and 0.6 save only 18,048 and 38,272 triangles respectively, so no production threshold is changed. See [rejection record](builds/democity_r5n_rejected.md).
- Golden-hour r5o tested increasing the existing environment-owned, sun-facing exposure compensation cap from 13% to 20%. Both inspected 17:30 captures remained zero-error but the skyline stayed sun/water-washed and city-side separation did not improve enough to warrant darker exposure. It is rejected and reverted. See [rejection record](builds/democity_r5o_rejected.md).
- Democity r5p accepts a roads-owner physical repair: a node-plateau blend had let the real avenue bridge surface fall to 2.4016 m even while its debug profile said 4.2 m. The shared road sampler now preserves the declared floor for every water-row consumer. Fresh public probing measures 4.2800 m / 4.1442 m for the two bridges, the 16-frame matrix is zero-error, and API/deserialize/tour/exact-restage contracts pass. Democity remains 6.0 FAIL; see [builder evidence](builds/democity_r5p.md) and [local review](critic/democity_r5p.md).
- Democity r5q freshly confirms the ground-seating failure: seed 1337 has 8 buildings over 0.25 m (maximum 0.711996 m) and its hospital landmark is 0.488396 m; seed 7 has 9 buildings over (maximum 2.319087 m). The existing building pad intentionally uses corners plus edge midpoints to prevent terrain penetration, while the failed measurement uses corners only. Lowering records would sink real walls, so no cosmetic or record-only fix is accepted. See [diagnosis](builds/democity_r5q_seating_diagnosis.md).
- Democity r5r measures only real traffic-owner activity around the ordinary street camera: 6 local vehicles at noon, 9 at 17:30 and 3 at 22:00. A global fleet increase would work against failed performance gates; a camera-local explicit loop would be showcase-only. No change is accepted. See [diagnosis](builds/democity_r5r_activity_diagnosis.md).
- Democity r5s closes only the stale civic-coverage clause after a fresh public recheck: seed 1337 is 306/507 = 60.36% health+education and seed 7 is 345/548 = 62.96%, both with public API/double-deserialize/tour success and zero errors. The score is held and all other issues remain. See [recheck](builds/democity_r5s_coverage_recheck.md).
- Democity r5t adds a reproducible, non-mutating real-city census. Both seeds have no free valid lots (612/612 and 648/648 occupied), remain below the 11,000-cell / 1,400-lot / 1,200-building scale gates, and have 202/209 buildings distributed across the unassigned outer ring. A giant fallback district label would not create real grain and is rejected. The inspected aerial remains a zero-error real city, but the 3.909M/33.5fps peak makes a genuine frontage expansion unsafe until its render cost is solved. See [diagnosis](builds/democity_r5t_cityscale_diagnosis.md).
- Democity r5v accepts the buildings owner’s intended LOD1 roof-clutter removal. It preserves real plans, silhouettes, atlas facades, shadows, API and exact restage; inspected noon/day/night evidence is clean. The 16-frame peak falls from 3,909,080 to **3,840,308 triangles**, but 3M/50fps and broader quality gates remain failed, so the local critic correctly holds **6.0 FAIL**. See [builder evidence](builds/democity_r5v.md) and [local review](critic/democity_r5v.md).
- Democity r5w re-profiles the accepted r5v main pass: props 894,676 triangles, roads 807,272, services 721,376 and buildings 451,032. Props LOD2 would save 215,876 but remains the rejected flat-impostor path; foliage/furniture contributes 243,460. See [profile](builds/democity_r5w_mainpass_reprofile.md).
- Democity r5x accepts an owner-safe volumetric props LOD1 reduction from 48 to 40 leaf cards. It keeps real trunk/species/atlas/shadow behavior and inspected noon/night volume; the clean matrix peaks at **3,827,780 triangles**, still above gate, so Democity remains **6.0 FAIL**. See [builder evidence](builds/democity_r5x.md).
- Democity r5z re-profiles roads on accepted r5x source. Gravel/embankment is 96,108 triangles, asphalt 48,738 and paint 35,788; all are zero-error. Gravel is already a minimal terrain-conforming strip and cannot be removed without floating edges. The concrete subtype mask missed its active LOD mesh and is explicitly invalid evidence. No change or score claim. See [profile](builds/democity_r5z_roads_reprofile.md).
- Services r6a tested a 720 m detail threshold and rejected it: no measured service chunk changed tier, so the scene gained no geometry reduction. API and the inspected aerial stayed clean, but no benefit justified changing the verified 800 m contract. The source is restored. See [rejection](builds/democity_r6a_services_lod_rejected.md).
- Democity r6b profiles the actual matrix peak at street 22:00: props 1,281,234 triangles, roads 1,168,784, services 469,168 and buildings 285,990. A first props subtype pass used invalid group labels; the corrected real-kind masks attribute 590,668 triangles to hard furniture and 978 to foliage.
- Democity r6c accepts a props-owned 420 m render distance for hard furniture and shelter glass. A 520 m trial moved no chunk. The accepted 16-frame matrix is zero-error at **799 draws / 3,735,080 triangles / 30.9 fps**; API, double deserialize, tour and exact restage pass, and all evidence was inspected. Score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6b_r6c.md) and [local review](critic/democity_r6c.md).
- Democity r6d repairs the profiler's roads concrete mask: it now covers detail and `-lod` meshes and survives owner visibility updates. Corrected street-22 attribution is 988,150 concrete triangles; the prior zero-delta result is invalid and superseded.
- Democity r6e keeps all visible road/PavementSurface data and ends only distant detail-concrete shadow submissions beyond 600 m. The inspected 16-frame matrix is zero-error at **799 draws / 3,312,992 triangles / 35.4 fps**; directed bridge views and 4.280000/4.144197 m clearance pass; API/restage pass. Score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6d_r6e.md) and [local review](critic/democity_r6e.md).
- Democity r6f re-profiles accepted r6e: props 1,188,534 triangles, roads 746,696, services 469,168 and buildings 285,990. Hard furniture is 497,968; species masks hide in shader and their zero triangle delta is explicitly not attribution evidence.
- Democity r6g replaces only the fully covered dark signal-lens face with a narrow bezel, reducing each of 643 real signal kits from 524 to 428 triangles while keeping the live lens/state. Custom close evidence and all 16 matrix frames were inspected; the zero-error peak is **799 draws / 3,267,680 triangles / 41.9 fps**, API/restage pass, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6f_r6g.md) and [local review](critic/democity_r6g.md).
- Democity r6h re-profiles accepted r6g: props 1,143,222 triangles, roads 746,696, services 469,168 and buildings 285,990 at street 22:00.
- Democity r6i retains the two heavy nearby service chunks and their shadows, while services-owned 128 m chunks beyond 420 m stop only shadow submission. All facilities, visible 800 m LOD, coverage and state remain. Directed evidence and all 16 matrix frames were inspected; the zero-error peak is **784 draws / 3,181,662 triangles / 40.7 fps**, API/restage pass, heap remains failed, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6h_r6i.md) and [local review](critic/democity_r6i.md).
- Democity r6j measures actual shadow-pass cost on accepted r6i source: props 531,684 triangles, services 294,474, roads 170,802, buildings 130,514, transit 31,008 and traffic 3,972. This is diagnostic evidence only; no score changes from attribution.
- Democity r6k applies the props owner's already-declared 120 m LOD1 shadow radius instead of the stale hardcoded 240 m value. Visible trees, near LOD0 contact shadows, live state and exact restage remain. Directed evidence and all 16 matrix frames were inspected; the zero-error peak is **778 draws / 3,155,766 triangles / 43.0 fps**, API/restage pass, heap remains failed, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6j_r6k.md) and [local review](critic/democity_r6k.md).
- Democity r6l repairs props shadow-class attribution: hard furniture submits 338,682 triangles, tree LOD0 131,940, tree LOD1 34,632 and foliage 534 at the r6k street-night peak.
- Democity r6m retains all visible furniture to 420 m and shadows in the three nearest 256 m chunks, while three chunks centred roughly 300 m away stop shadow submission. Six directed and all 16 matrix images were inspected; the zero-error peak is **775 draws / 3,088,020 triangles / 44.6 fps**, API/restage pass, heap remains failed, and the score remains **6.0 FAIL**. Do not lower this radius again under the current coarse chunk contract. See [builder evidence](builds/democity_r6l_r6m.md) and [local review](critic/democity_r6m.md).
- Democity r6n proves building shadow distance must be horizontal: using camera height would strip nearly every aerial shadow. Buildings submit 130,514 shadow triangles at the r6m peak; a 400 m horizontal mask removes 84,656 in diagnosis while retaining the central shadow neighbourhood.
- Democity r6o keeps all visible building geometry and building state, while 128 m chunks beyond 400 m horizontally stop only shadow submission. Six directed and all 16 matrix images were inspected; the zero-error peak is **532 draws / 3,006,462 triangles / 44.9 fps**, API/restage pass, heap remains failed, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6n_r6o.md) and [local review](critic/democity_r6o.md).
- Democity r6p measures 384 m first (3,002,124 triangles, still failed), then accepts the next actual chunk transition at 360 m. All visible buildings and the nearest 20 shadow chunks remain. Six directed and all 16 matrix images were inspected; the zero-error peak is **532 draws / 2,997,270 triangles / 45.4 fps**, so the composed 3M gate passes by 2,730. API/restage pass, heap remains failed, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6p.md) and [local review](critic/democity_r6p.md).
- Democity r6q attributes retained CPU storage after forced GC: buildings 220.9MB, props 52.8MB, services 43.9MB and roads 22.2MB. Buildings duplicated every simple/shadow range between two mutually exclusive LOD meshes.
- Democity r6r shares one immutable full/simple/shadow buffer per building chunk while preserving separate draw-range callbacks. Forced LOD totals remain exactly 977,816/200,332; all 16 matrix images are clean at **532 draws / 2,997,270 triangles / 45.8 fps**. Buildings buffers fall to 176.7MB, total backing storage to 475.4MB and clean reported heap to 510.0MB. Post-restage forced-GC reported heap remains 541.7MB, so the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6q_r6r.md) and [local review](critic/democity_r6r.md).
- Democity r6s replaces noisy short timing with stable 180-frame controls. Terrain, props and roads are the largest real render-time owners. More specifically, a fixed aerial-night camera averages about 1.910M submitted triangles at speed 0 but 2.354M at speed 1 because animated water invalidates the half-resolution planar reflection every frame. This is diagnosis only; no production or score change. See [profile](builds/democity_r6s_frame_profile.md).
- Democity r6t keeps per-frame water-surface animation but caps fixed-camera active planar-geometry refresh at every fourth frame. Camera motion and explicit invalidation remain immediate. The cadence probe passes 15/30/120 paused-fixed/active-fixed/active-moving reflections over 120 frames; active aerial-night rises from 39.72–41.89 to 43.64–44.46 fps. Directed and all 16 matrix images are clean; peak stays 2,997,270 triangles, contracts/restage pass, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6t.md) and [local review](critic/democity_r6t.md).
- Democity r6u proves continuous-camera reflection costs about 4 fps, while disabling it still reaches only 45.7 fps; global terrain nofine/nomacro variants do not help and are rejected. r6v reduces only High's planar target from 768×384 to 640×320. Exact A/B differs 0.021–0.039%, the moving-camera controls gain about 0.5–1.2 fps, all 16 images/contracts remain clean, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6u_r6v.md) and [local review](critic/democity_r6v.md).
- Democity r6w identifies buildings as the largest remaining non-reflection owner; r6x's ORM sample reuse is rejected for no measured gain. r6y stores static building normals/UVs and exact tint slots in 16 bits. Buildings buffers fall 176.7→145.2MB; clean/post-restage forced-GC heap is 480.0/509.9MB, both below gate. Exact LOD totals, 16 images and contracts pass; 44.1 minimum fps still fails, so the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r6w_r6x_r6y.md) and [local review](critic/democity_r6y.md).
- Democity r6z finds no repeatable benefit from removing all building shadows, while all service shadows provide a 1.6–2.5 fps upper bound. r7a tests real footprint/height proxies only for 220–420 m service shadows: peak triangles fall by 84,878, but 47.86–48.86 fps remains within baseline variation, draw count does not fall and shadow shape becomes coarser. It is rejected and fully reverted. See [profile and rejection](builds/democity_r6z_r7a_rejected.md).
- Democity r7b finds a 49.67–49.74 fps global-building-LOD1 upper bound, but r7c/r7d's owner-safe 72 m and 60 m thresholds remain inside baseline timing variation. r7e normal/ORM/emissive map masks are likewise inconclusive and visually required. Every candidate is rejected; the accepted 84 m LOD and full material are restored. See [profile and rejections](builds/democity_r7b_r7c_r7d_r7e_rejected.md).
- Democity r7f reproduces the cross-seed lifecycle defect: seed-7 plans were rebuilt over seed-1337 terrain. r7g adds terrain-owned deterministic regeneration and a roads-owned new-world reset while preserving ordinary deserialize IDs. Fresh and restaged seed 7 now match across all 263,169 terrain samples, features, road topology, counts and districts. The correct larger world initially failed memory at 528.4–529.4MB, so r7h compresses only static service attributes and lowers service buffers 43.95→22.66MB. The final seed-7 forced-GC result is **509.1MB**; 16/16 images are clean at **532 draws / 2,997,270 triangles / 48.1 fps**, API/restage cycles pass, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r7f_r7g_r7h.md) and [local review](critic/democity_r7h.md).
- Democity r7i rejects two terrain-foundation prototypes. Full footprint pads close the seating metric but hit 5,337–5,986 road cells and raise terrain by up to 19.82m. Narrow eight-support edits still hit 2,195–2,422 road cells and leave two seed-7 failures, including a new 3.60m gap. Production remains unchanged; a safe repair needs coordinated pre-road reservations or a physical retained foundation contract. See [rejection record](builds/democity_r7i_foundation_rejected.md).
- Whole-game r7j closes the verified partial-world load path. Core snapshots every incoming target owner before mutation, stops at the first refusal and reapplies the prior dependency-ordered state. A forced late Transit rejection restores all 15 canonical module payloads, terrain, time and camera exactly, emits no false `save:loaded`, and a following valid load succeeds. Buildings also restores its allocator idempotently. Democity API/restage pass and an inspected Metal smoke has zero errors. See [builder evidence](builds/save_atomicity_r7j.md) and [local review](critic/integration_save_atomicity_r7j.md).
- Whole-game r7k corrects the measured ordinary-play economy without grants. Private zoned buildings remain tax sources instead of municipal upkeep liabilities, and administration scales with population. The earned 404-resident save remains solvent for 30 deterministic days at 10/15/20% tax and for 90 days at 15/20%; exact repeat output matches. At default 10% it needs player action before day 33, while population reaches 465 and then needs new zoning. Human pacing remains unverified. See [builder evidence](builds/play_balance_r7k.md) and [local review](critic/integration_play_balance_r7k.md).
- Democity r7l applies an environment-owned display factor to the completed visible dome at night while preserving the PMREM radiance input. The 22:00 skyline improves from mean/median/highlight ratio 59.44/49.69/3.33 to 41.01/39.04/4.24. All 16 standard and two directed night captures are zero-error and inspected; API and exact restage pass. Aerial/skyline bright-source density still fails at 0.638/0.664%, performance still misses 50 fps, and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r7l_night_sky.md) and [local review](critic/democity_r7l.md).
- Democity r7m confirms public traffic density does not repair sparse street-camera distribution and adds no showcase fleet. r7n rejects a broad sky-aureole scalar after it fails to improve the image. See [diagnosis and rejection](builds/democity_r7m_r7n_rejected.md).
- Democity r7o attributes the 17:30 wash chiefly to the visible sky/horizon in one frozen page. r7p accepts an environment-owned low-sun dome/exposure adjustment: deterministic skyline pixels above luma235 fall from **12.0201% to 1.4606%**, but its upper-sky flat patch remains **6.1111%** against `<5%`. Three other required views pass, build/API/exact-restage pass and all images are inspected. A pre-cloud compression that created hard cut-out clouds was rejected. Score remains **6.0 FAIL**. See [builder evidence](builds/democity_r7o_r7p_golden_hour.md) and [local review](critic/democity_r7p.md).
- Democity r7q re-runs the stale aerial autocorrelation blocker against current source with a deterministic implementation. Aerial noon now passes at column/row **0.538390/0.392787**, and aerial golden plus overview noon also pass the 0.55 ceiling. Exact repeat is byte-identical and all three zero-error images are inspected. No production or score change; asset variety and district grain remain separate failures. See [recheck](builds/democity_r7q_autocorrelation_recheck.md).
- Democity r7r weights real traffic origins and pedestrian sidewalks by occupied zoned frontage while preserving fleet totals, routes, collisions and save ownership. The standard noon target now contains **13 vehicles / 7 kinds / 12 pedestrians** versus required12/5/8; same-seed state is exact, seed7 differs, and restore plus 30-second route/gap checks pass. All16 standard and3 directed images are inspected with zero errors at **532 draws / 2,998,792 triangles / 43.2fps**. The numeric clause closes, but composition/night activity and the50fps gate remain failed, so the score stays **6.0 FAIL**. See [builder evidence](builds/democity_r7r_activity_weighting.md) and [local review](critic/democity_r7r.md).
- Democity r7s rejects two real scale-layout candidates. A64m grid produces515 nodes/759 edges but only573 buildings; an80m no-split grid produces695 buildings but only342 nodes. Both remain far below1,200 buildings/1,400 lots and are restored. Closing rank1 safely now requires coordinated frontage/lot-layout and render-storage work rather than another bounded layout scalar. See [rejection record](builds/democity_r7s_scale_layout_rejected.md).
- Democity r7t profiles the accepted slow street view and sets High terrain LOD distance to the established Medium0.65 threshold. Two240-frame pairs show small consistent gains, the16-frame peak falls **2,998,792→2,941,576 triangles**, and min fps is44.9. All16 standard plus interchange images are inspected without new seams; API/exact restage pass. The50fps gate and score remain **6.0 FAIL**. See [builder evidence](builds/democity_r7t_terrain_lod.md) and [local review](critic/democity_r7t.md).
- Democity r7u profiles props and reduces only hard furniture/glass visibility from420m to280m. The slow view retains seven chunks within251m and culls one centred at298m; no world records change. All16 standard plus4 A/B views are inspected, peak falls **2,941,576→2,918,778 triangles**, while min44.2fps still fails. Do not lower the coarse256m chunk distance again. Score stays **6.0 FAIL**. See [builder evidence](builds/democity_r7u_props_furniture_distance.md) and [local review](critic/democity_r7u.md).
- Democity r7v profiles roads/services on accepted r7u source. Road/service shadow masks provide no timing benefit. A640m service-detail candidate is visually rejected for monolithic facility proxies and restored to800m. The accepted roads-owned concrete threshold700m moves only one743m street tile to its existing top-face LOD; pavement/network/bridges/state remain unchanged. All16 standard plus7 directed comparisons are inspected, peak falls **2,918,778→2,884,298 triangles**, min39.6fps still fails, and API/restage pass. Score stays **6.0 FAIL**. See [builder evidence](builds/democity_r7v_roads_services_profile.md) and [local review](critic/democity_r7v.md).
- Democity r7w attributes the remaining wide-night bright-pixel failure with geometry retained. Services contributes only0.0255/0.0077 percentage points in aerial/skyline; props and traffic are negligible. Building light changes mean luminance but no pixels over180 at the measured scale. Temporary far-window floors12% and25% leave both failed fractions unchanged and are rejected; production remains7%. No source or score change. See [diagnosis](builds/democity_r7w_night_source_diagnosis.md).
- Whole-game r7x closes successful-load Transit continuity. Same-state and changed-state restores preserve Transit/Simulation payloads, route arrays, lengths, headways, fleet, ridership, balance, current economy and the next deterministic day exactly. The probe found and repaired a future `nextLine` allocator leak; the saved allocator now restores above the maximum live ID. The existing deliberate late-owner rejection still rolls all14 serialized owners, terrain, time and camera back exactly. Transit remains **6.8 FAIL** and Democity **6.0 FAIL**. See [builder evidence](builds/transit_load_continuity_r7x.md) and [local review](critic/integration_transit_load_r7x.md).
- Whole-game independent r1 reviews35 fresh current-source images across every declared camera, four times,720p and the requested weather URLs. Score is **6.0/10 FAIL**:469 draws and2,788,342 triangles pass, but43.7fps fails; primitive landmarks/facility proxies, sparse frontage, flat foliage, shallow night lighting and grading/contact lead the visible gap. Independent blind A/B is **SimBuild0–4 CS2**, all high confidence. See [independent whole-game review](critic/wholegame_r1_independent.md) and [blind review](critic/blind_r1_independent.md).
- Democity r7y fixes the independent critic's false-weather path. The city applies its prior custom weather only without an explicit URL request, leaving real cloudy/rain presets with environment. Contract values pass; four Metal captures are zero-error and inspected; default p95 pixel delta is0 while cloudy/rain differ over83%/85% of pixels. API and exact cross-seed restage pass. Democity stays **6.0 FAIL** and whole-game **6.0 FAIL**. See [builder evidence](builds/democity_r7y_weather_contract.md) and [local review](critic/democity_r7y.md).
- Democity r7z inventories every declared camera and confirms that downtown's seven distant paid services used only84 triangles as one box each. The accepted owner-safe renderer keeps the800m boundary and real IDs/footprints/state, but uses kind-specific merged silhouettes. Inspected downtown/interchange crops replace blank halls with recognizable plant/civic volumes; downtown final geometry rises only816 triangles with unchanged430 draws and51.3fps versus52.0 control. API, exact restage and rollback pass. Scores stay **6.0 FAIL** because primitive landmarks, sparse frontage and broader gates remain. See [builder evidence](builds/democity_r7z_service_proxy_silhouettes.md) and [local review](critic/democity_r7z.md).
- Democity r8a replaces the existing arena landmark's closed cylinder/hemisphere with an open field, flat panelled roof ring, fascia, facade piers and entrance. Thick/floating torus trials are visually rejected before the accepted v5. The exact13-landmark plan and8 owner draws remain; landmark geometry rises only3,600→4,076 triangles. Noon/night/wide captures, API and exact restage pass. Scores remain **6.0 FAIL** because other assets and the sparse arena site still fail the independent quality bar. See [builder evidence](builds/democity_r8a_arena.md) and [local review](critic/democity_r8a.md).
- Democity r8b selects 14 real high-density central avenue lots for the existing building-owner mixed-use programme: 11 apartments and three towers now have real retail bases, occupants/jobs and save state. Failed zero/three-building orderings remain as evidence. Its 16-frame matrix is zero-error at **473 draws / 2,258,952 triangles / 43.2 fps**. A new double-restore probe caught stale zoning-lot ownership and zero loaded office jobs; zoning, buildings and simulation now restore both exactly, and whole-owner rollback still passes. Scores remain **6.0 FAIL** because 612 lots/buildings still miss scale, 50fps fails and broader frontage/asset/grounding/foliage quality remains open. See [builder evidence](builds/democity_r8b_mixed_use_frontage.md) and [local review](critic/democity_r8b.md).
- Democity r8c keeps the same Southbank hospital plan/footprint/seat and gives its previously blank north/east elevations window rhythm, a sheltered entrance, medical mark and roof plant. The full 16-frame matrix is zero-error at **473 draws / 2,260,248 triangles / 44.9 fps**; the merged landmark pass stays at eight draws and rises only 4,076→4,724 triangles. API and exact restage pass. Scores remain **6.0 FAIL** because the adjacent water tower, port assets and broad production-quality gaps remain. See [builder evidence](builds/democity_r8c_hospital.md) and [local review](critic/democity_r8c.md).
- Democity r8d keeps the same Birch water-tower plan/footprint/seat and replaces its blank tank/four posts with a flared banded tank, catwalk, ladder, riser and cross-braced frame. The 16-frame matrix is zero-error at **473 draws / 2,262,280 triangles / 45.0 fps**; the owner remains eight draws and rises 4,724→5,740 triangles. API and exact restage pass. Scores remain **6.0 FAIL** because port assets, scale,50fps and broader art/site gaps remain. See [builder evidence](builds/democity_r8d_water_tower.md) and [local review](critic/democity_r8d.md).
- Democity r8e keeps the same two Northbank crane records and replaces their flat two-post silhouettes with four-legged portals, bracing, booms/stays, cabs, trolleys and hoists. The 16-frame matrix is zero-error at **473 draws / 2,264,008 triangles / 48.4 fps**; the owner remains eight draws and rises only 5,740→6,172 triangles. API and exact restage pass. Scores remain **6.0 FAIL** because port silos/apron, foliage, scale,50fps and broader site quality remain. See [builder evidence](builds/democity_r8e_port_cranes.md) and [local review](critic/democity_r8e.md).
- Democity r8f profiles the real park/waterfront tiers, changes LOD1 from 40 large cards to 72 smaller cards and gives each of five distance classes a second deterministic side silhouette. Top-down cells/rules are unchanged. Three ineffective or visually bad trials are retained and reverted. The final 16-frame matrix is zero-error at **473 draws / 2,264,008 triangles / 47.7 fps**; API and exact restage pass. Scores remain **6.0 FAIL** because park sparsity, planar distance foliage, scale,50fps, grounding and night depth remain. See [builder evidence](builds/democity_r8f_foliage_lod.md) and [local review](critic/democity_r8f.md).
- Fresh post-r8f whole-game r2 inspects all35 current-source captures and holds **6.0 FAIL**. The32-frame matrix is zero-error at **473 draws / 2,807,186 triangles / 46.2 fps minimum**;14/32 frames miss50fps. The accepted r7z/r8a-r8f work is visible, while sparse block fabric, planar foliage, shallow night lighting, road/terrain grades, schematic sites, layered horizon/weather and thin life still determine the full-frame result. See [independent review](critic/wholegame_r2_independent.md).
- Fresh blind r2 again returns **SimBuild0–4 CS2**, all four at high confidence. The decoded losses independently identify weak occupied parcel ground/frontage, flat foliage, road-to-terrain shoulders and locally varied night storefront/entry lighting. See [blind review](critic/blind_r2_independent.md) and `../shots/integration/blind-r2-key.json`.
- Democity r8g diagnoses the rank1 parcel limit and extends the existing real mixed-use owner programme from14 to all32 eligible high-density residential/office avenue lots inside300m. It adds no fake/duplicate buildings and keeps612 occupied lots, but owner respawn changes18 building IDs/forms plus cash/net, so identical state is not claimed. All25 original images are independently inspected and zero-error; the16-frame matrix remains inside geometry at **469 draws / 2,808,896 triangles** but samples **35.3fps minimum**. The independent critic weakly accepts the local frontage gain while keeping **6.0 FAIL** and rank1 open because only48.996% of painted cells are claimed. See [builder evidence](builds/democity_r8g_frontage_corridor.md) and [independent review](critic/democity_r8g.md).
- Democity r8h measures the elevated park at **0 LOD0 / 160 LOD1 / 2,175 impostors** and keeps the canonical LOD1 budget at72 cards/168 triangles. It redistributes24 cards into an inner radial layer while48 retain the outer layer. All21 original plus6 derived images are independently inspected; the crown core is modestly more continuous with no definite new opacity/silhouette regression. The16-frame matrix is zero-error at **469 draws / 2,264,630 triangles / 46.2fps minimum**. The critic accepts only the local gain and keeps **6.0 FAIL** and foliage rank2 open. Top-down imagery, full props-world restore and temporal LOD behavior are not certified. See [builder evidence](builds/democity_r8h_foliage_core.md) and [independent review](critic/democity_r8h.md).
- Democity r8i keeps the existing baked window state and adds stable low cabinet/partition occlusion shapes to ordinary and curtain-wall atlas variants0/2. No RNG, lights, geometry, IDs, simulation or save state change. All26 originals and sidecars are independently inspected; the near view gains a modest occupied-room cue, while wide grids and fixture/facade/ground cohesion remain weak. The16-frame matrix is zero-error at **469 draws / 2,808,896 triangles / 42.6fps minimum**;7/16 miss50fps. API, double deserialize, all32 mixed-use owner records and exact seed1337/7 authored restage pass. Score remains **6.0 FAIL** and rank3 stays open. See [builder evidence](builds/democity_r8i_night_depth.md) and [independent review](critic/democity_r8i.md).
- Democity r8j retains the same saved Northbank `port-7` apron record and replaces its pale slab/faint divisions with a darker structured hardstand, edge beams, five stronger divisions, five fixed wheel stops and seven posts inside the same46×30m footprint. All26 originals and sidecars are independently inspected; owner accounting remains eight draws and rises only6,172→6,760 triangles. The16-frame matrix is zero-error at **469 draws / 2,809,484 triangles / 46.2fps minimum**;7/16 miss50fps and raw heap peaks730.3MB. API, double deserialize, mixed-use32 and exact seed1337/7 authored restage pass. This is static landmark detail, not verified cargo, driveability or a functioning quay. Score remains **6.0 FAIL** and rank5 stays open. See [builder evidence](builds/democity_r8j_apron.md) and [independent review](critic/democity_r8j.md).
- Democity r8k retains the same three saved14×18m Northbank silo records and adds fixed seams, coarse access fittings, small landings and roof vents inside their footprints. All24 originals and sidecars are independently inspected; exact renderer replay keeps eight draws and raises owner geometry6,760→7,912 triangles, +384 per silo. The16-frame matrix is zero-error at **469 draws / 2,814,092 triangles / 46.5fps minimum**;2/16 miss50fps and raw heap peaks732.4MB. API, double deserialize, mixed-use32 and exact seed1337/7 authored restage pass. The1.45m rung spacing remains schematic and no storage/cargo/port operation is claimed. Score remains **6.0 FAIL** and rank5 stays open. See [builder evidence](builds/democity_r8k_silos.md) and [independent review](critic/democity_r8k.md).
- Democity r8l publishes environment's existing night factor through the shared shader contract and applies the visible dome's existing0.25 night display gain to shared fog plus terrain-water sky reflection/horizon samples. Component masks attribute the glaring22:00 strip to that mismatch; independent pixel replay measures horizon-minus-sky +47.70878→−2.15514. All29 originals are independently inspected. The16-frame matrix is zero-error at **469 draws / 2,814,092 triangles / 46.1fps minimum**;2/16 miss50fps and raw heap peaks741.5MB. Four cloudy/rainy controls are zero-error but32.8–44.5fps. Shared night0/0/1/0/1, water cadence15/30/120, API, double deserialize, mixed-use32 and exact seed1337/7 restage pass. Score remains **6.0 FAIL**: the straight geometric and17:30 seams, mountain/weather art and performance remain open. See [builder evidence](builds/democity_r8l_night_horizon.md) and [independent review](critic/democity_r8l.md).
- Democity r8m applies the visible dome's existing directional low-sun display gain to water reflection/horizon samples and shared distance fog while retaining the raw physical LUT for PMREM and direct light. Independent pixel replay changes the pinned17:30 horizon-minus-sky from +82.86399 to −1.61172. All39 originals are independently inspected; the16-frame matrix is zero-error at **469 draws / 2,814,092 triangles / 41.6fps minimum** with3/16 below50fps and733.7MB raw heap. API, double deserialize, mixed-use32, exact seed1337/7 restage and water cadence15/30/120 pass. Score remains **6.0 FAIL**. The improved sweep exposes a separate preexisting black frame around18:00 from non-finite sun colour, now the next P1. See [builder evidence](builds/democity_r8m_low_sun_horizon.md) and [independent review](critic/democity_r8m.md).
- Democity r8n raises the near-horizon solar transmittance sample floor0.004→0.008 and guards solar-colour normalization with the actual transmittance maximum. It removes the reproduced NaN-contaminated fog and black scene in all tested boundary states. All39 originals are independently inspected; all20 sweep pixel metrics reproduce within1e-9 and three fresh17.99/18/18.01 starts are zero-error. The16-frame matrix remains within geometry at **469 draws / 2,814,092 triangles / 47.2fps minimum**, with4/16 below50fps and682.5MB raw heap. API, double deserialize, mixed-use32, exact seed1337/7 restage, display and cadence15/30/120 contracts pass. Score remains **6.0 FAIL**; the warm-sun/cool-moon handoff, city fabric, mountain art and performance remain open. See [builder evidence](builds/democity_r8n_sunset_nan.md) and [independent review](critic/democity_r8n.md).
- Democity r8o tested four bounded mountain-material candidates: rock-coordinate remapping, weaker macro mask influence, a secondary rock sample and darker scree. The layer diagnostic attributes much of the pale coarse pattern to scree, but none of the candidates fixes the geological read; all are rejected and reverted. The independent critic inspected all27 originals, reproduced matched metrics and verified restored `material.js` SHA256 `fcc1c60e31f321d72be524789ac53da31f0bcdffd68419dc29c04c0ff41478b0`. City-frame render counters are not matched performance evidence. Score remains **6.0 FAIL**. See [rejection record](builds/democity_r8o_mountain_material_rejected.md) and [independent audit](critic/democity_r8o.md).
- Democity r8p replaces Props' per-signal calls into Traffic with one owner-provided scalar change key while keeping the detailed accessor as a fallback. Independent direct/static/moving callback means fall **98.168850% / 97.177849% / 96.014493%**; all21 originals, exact source snapshots and16 sampled transition keys are audited. This is a bounded CPU acceptance, not a general FPS or memory pass: the final matrix still reaches34.0fps, a directed night frame29.2fps and repeat raw heap692.9MB. Score remains **6.0 FAIL**. See [builder evidence](builds/democity_r8p_signal_key.md) and [independent audit](critic/democity_r8p.md).
- Democity r8q adds one early return after Traffic has gated and zero-accounted its hidden fallback-mast group. Independent static/moving/direct callback reductions are **55.914653% / 55.327869% / 56.767768%**. All18 originals and all16 exact R8p/R8q geometry pairs pass; the visible180-lens Traffic fallback continues updating with equal stored32-bit buffer hashes. Static/moving FPS disagree, final raw heap reaches708.3MB and headless readback dominates the sampled trace, so scores remain **6.0 FAIL**. See [builder evidence](builds/democity_r8q_hidden_masts.md) and [independent audit](critic/democity_r8q.md).
- Democity r8r is accepted as diagnosis only. On one warmed accepted-source page, masking interchange LOD1 tree meshes gives a stable synchronized **+9.255991%** median response with 3/3 control drifts below5%; park is weaker and pool/lens/halo/impostor/furniture evidence does not outrank it. The mask removes complete mesh work, including draw, vertex, raster and possible shadow contribution, so it does not prove shading alone. The no-headless control changes broader browser policy and is not a readback-only experiment. No screenshots or product source are accepted and scores remain **6.0 FAIL**. See [diagnosis](builds/democity_r8r_render_attribution.md) and [independent audit](critic/democity_r8r.md).
- Democity r8s tests a separate Lambert colour material on the exact LOD1 mesh set, then **rejects and exactly reverts** it. The first3-cycle A/B has −0.261270% median and1/3 stable controls; the five-cycle repeat has +1.618821% but only4/5 stable controls, two negative responses and +0.633417% stable-only median. Toon is visibly brighter/flatter and the first Lambert smoke retains its shader failure as invalid evidence. The independent critic verifies all nine hashes and restored production bytes. Scores remain **6.0 FAIL**. See [rejection](builds/democity_r8s_lod1_material_rejected.md) and [independent audit](critic/democity_r8s.md).
- Democity r8t reads both accepted seed worlds and accepts **no product change**. Seed1337/7 have5,082/4,937 unclaimed painted cells and only706/675 beyond preferred depth. Class-only pooling gives1,229/1,244 theoretical lots, while a stricter stored-edge/side component model gives804/789. Independent audit verifies available arithmetic but finds the model stricter than the real `_slots()` sampler, so those values are conditional estimates and never capacity ceilings. Score remains **6.0 FAIL**. See [diagnosis](builds/democity_r8t_parcel_capacity.md) and [independent audit](critic/democity_r8t.md).
- Democity r8u replays the actual ordered zoning slots and emitted lot memberships for both accepted seeds. The independent audit verifies1,180 edges,15,196 slot tokens,1,447 full-depth runs,1,260 lots and10,073 raw membership references. It finds5/6 within-lot duplicate references and2 cross-lot overlap keys per seed; every overlap is currently resolved to the later claimant, while three later lots use an overlap as their stable first key. The diagnostic is accepted at unchanged **6.0 FAIL** and permits only a bounded last-owner-preserving normalization with explicit identity, building/economy, regeneration, load and boundary checks. See [diagnosis](builds/democity_r8u_slot_order.md) and [independent audit](critic/democity_r8u.md).
- Democity r8v records each generated lot's original first cell for stable identity, then normalizes public memberships only after the existing last-winner claim pass. Independent review verifies7/8 removed references, zero remaining duplicates/overlaps, unchanged612/648 lots/buildings, exact lot frames/IDs, buildings, roads, serialized simulation/economy, two refreshes, two restores and cross-seed restage. All seven originals are inspected; directed overlay geometry hashes/counts are exact. The score remains **6.0 FAIL** because capacity, city fabric and performance do not improve. Distinct historical import, a stolen-first-key fixture and regeneration cost remain untested. See [builder evidence](builds/democity_r8v_lot_membership.md) and [independent review](critic/democity_r8v.md).
- Democity r8w retains and independently reconstructs all15,196 actual frontage-slot key sequences and first stop reasons. The current order yields612/648 lots from2,048/2,116 full-depth slots;544/506 full-depth slots remain unabsorbed, including419/394 in too-short runs. Prior claims are the largest first-failure class among shallow painted slots at856/930, but3,718/4,073 first-cell samples are unpainted and64/63 class-change stops are also claimed. Only3/3 adjacent key overlaps and no identical adjacent columns reject a simple column-dedupe path. This is diagnosis only at **6.0 FAIL**. See [builder evidence](builds/democity_r8w_raw_slot_keys.md) and [independent review](critic/democity_r8w.md).
- Democity r8x tests four deterministic within-road-class claim orders on both accepted seeds and rejects them all. ID order gains8/8 lots but replaces129/130 accepted stable keys; the other variants gain1–9 while replacing78–225. Every alternative changes building and simulation serialization plus economy. Independent review accepts the rejection at unchanged **6.0 FAIL** and records that these fresh-world trials do not prove historical-save failure, visible harm or exhaustion of every adaptive/localized strategy. See [rejection record](builds/democity_r8x_frontage_order_rejected.md) and [independent audit](critic/democity_r8x.md).
- Democity r8y screens316 existing-node/cardinal pairs without adding a road. Its road-mask heuristic retains19/23 gap candidates; only1/2 also have zero accepted-contact proxy, dry terrain and no sample above the zone-paint slope filter, and all predict zero lots. Positive estimates are at most1/2 lots with parcel-contact or slope risk. Independent review accepts this only as a qualified diagnosis at **6.0 FAIL**: no actual road, lost parcel or universal local-design impossibility is claimed. The accepted-source smoke is errors0 at430 draws/2,061,474 triangles/54.6fps. See [builder evidence](builds/democity_r8y_local_frontage.md) and [independent review](critic/democity_r8y.md).
- Democity r8z tests one disposable LOD1 primary-branch whorl and rejects it without changing product source. It adds three branches and raises canonical LOD1 geometry **308 vertices / 168 triangles → 368 / 240**, while matched park and riverfront frames differ by only **0.0060–0.0244% normalized MAE**. The branches are slightly visible on exposed trunks, but separated leaf clumps, flat crowns, impostor silhouettes and dark night crowns remain. Independent review accepts the rejection record at **6.0 FAIL**; this does not reject every possible branch treatment. See [rejection record](builds/democity_r8z_lod1_branches_rejected.md) and [independent audit](critic/democity_r8z.md).
- Democity r9a raises only Props’ normal-camera LOD1 range **175→205 m**, retaining the accepted 168-triangle crown, ±12m transition half-width, normal `CAP1=520` guard, records, placement, materials, atlas, saves and RNG. The matched park view gains 44 submitted LOD1 histogram entries and visibly replaces its nearest flat silhouettes; fade-inclusive histograms do not prove 44 unique visible trees. All24 originals and four crop pairs are independently inspected. The final16-frame matrix is zero-error at **468 draws / 2,275,778 triangles / 49.5fps minimum / 730.2MB raw heap**; one frame misses50fps and memory still fails. Score remains **6.0 FAIL**. See [builder evidence](builds/democity_r9a_lod1_range.md) and [independent review](critic/democity_r9a.md).
- Democity r9b makes no product change and independently accepts only a qualified LOD diagnosis. Eleven forced-refresh states and six inspected originals show no definite missing/material defect;168 monotonic movement frames peak at queue44 and end queue0, but their final histogram differs from a forced255m refresh. Ordinary direct demand peaks261, while forced debug LOD observes the520 guard without a valid same-update source partition. Immediate camera/forced-refresh ordering also has a world-matrix synchronization risk. Score remains **6.0 FAIL**; repair the verification ledger before any further range/CAP change. See [builder diagnosis](builds/democity_r9b_lod1_transition.md) and [independent audit](critic/democity_r9b.md).
- Democity r9c repairs that verifier without changing product source. Five synchronized automatic distances retain5,717 actual copied membership rows with zero tier/boundary mismatches. A175-frame monotonic flight ends with queue0; the ordinary0/117/2,475 endpoint differs from exact0/114/2,477 because Props last evaluated the camera5.197786m earlier, inside its existing6m update threshold. The synchronized disposable cap update partitions all2,295 observed entries as520 LOD1 +1,775 impostors, with no fade or duplicate outputs, and its screenshot is taken after three rendered frames. Independent review accepts the tooling repair at **6.0 FAIL**. The ledger does not independently enumerate source buckets omitted before `_copy`, covers no distance below72m and uses direct disposable-field `forceLod`; retain those limits. See [builder evidence](builds/democity_r9c_lod1_ledger.md) and [independent review](critic/democity_r9c.md).
- Democity r9d independently accepts a synchronized representation diagnosis and rejects a disposable impostor `alphaTest 0.42→0.50` candidate. One real130-tree/53-bucket park chunk is isolated below unchanged CAP1 across four daylight azimuths and night; exact selector reconstruction places9–18 in-frame trees in the real193–217m band. LOD1 is coarse and separated, while impostors are planar and darker on70.76–77.87% of strong daylight difference pixels. Alpha erosion reduces same-page mismatch only3.56–4.64% and leaves the defect visible; night drift prevents a causal night claim. Product source is unchanged and the score remains **6.0 FAIL**. See [builder evidence](builds/democity_r9d_lod1_impostor.md) and [independent review](critic/democity_r9d.md).
- Democity r9e splits only the broad/narrow procedural side silhouettes into three deterministic lobes while preserving their 520/420 primitive totals, RNG position, four-triangle geometry and all LOD/material/state contracts. Independent review verifies a **14.00–16.35% daylight MAE reduction** against LOD1 at exact193–217m selector buckets and accepts the modest visible improvement at unchanged **6.0 FAIL**. Whole-atlas filtering also changes142 pixels in top narrow cell7, including135 alpha changes and63 nominal alpha-test crossings; this is a real bounded containment defect, not a claimed top-view invariant. The16-frame matrix remains zero-error at46.1fps minimum and756.4MB raw heap, so strict FPS/memory gates fail. See [builder evidence](builds/democity_r9e_side_atlas.md) and [independent review](critic/democity_r9e.md).
- Democity r9f repairs its own verification path and isolates only top narrow cell7 with clear-before-copy plus a live-product RGBA oracle. Every contained state reproduces exactly142 changed pixels,135 alpha changes,63 nominal alpha0.42 crossings and the documented bounds; product/replay atlas bytes are exact. The600m actual-distribution and amplified controls have pixel-exact screen replay and show sparse canopy-edge flecks, but all originals are visually indistinguishable. Independent review accepts verification-only closure, makes no product/performance claim and retains **6.0 FAIL**. Earlier whole-atlas and source-over summaries remain rejected harness evidence. See [builder evidence](builds/democity_r9f_top_containment.md) and [independent review](critic/democity_r9f.md).
- Democity r9g routes one real two-lane street through the Roads owner only in disposable responses and rejects it after actual frontage generation. Existing road edge payload/order remains exact and both seeds repeat deterministically, but seed1337 loses one lot/building and seed7 replaces3 keys for zero net stock; claimed cells fall12/6. All8 originals and4 differences are inspected. Independent review accepts the qualified rejection at **6.0 FAIL**: R8y used alley calibration, so narrower geometry remains untested, and fresh-start ID differences do not prove incremental regeneration failure. See [rejection record](builds/democity_r9g_actual_frontage_rejected.md) and [independent review](critic/democity_r9g.md).
- Democity r9h independently accepts only the fresh-start allocation diagnosis at **6.0 FAIL**. All four captured worlds have two observed lot-generation calls: an empty post-road call and one complete empty-to-painted allocation. The candidate changes only edge335 in seed1337 and edges348/346 in seed7; global sequential IDs explain exactly187/126 changed retained lot IDs. This is not an incremental/load defect or product change. See [builder diagnosis](builds/democity_r9h_allocation_trace.md) and [independent review](critic/democity_r9h.md).
- Democity r9i tests the same endpoint pair as a real alley through disposable Roads/Zoning owners. It yields +1/+2 net lots and repeats exactly with zero errors; all10 originals and4 differences are independently inspected. The public Tools API rejects the exact72m link on both seeds at20.80% and12.32% grade against the12% limit, and seed7 changes two retained parcel geometries despite their stable keys. Independent review accepts rejection without product or score change. See [rejection record](builds/democity_r9i_actual_alley_rejected.md) and [independent review](critic/democity_r9i.md).
- Democity r9j pre-screens the second common positive R8y link through the public Tools API. The exact80m alley passes at9.73%/7.36%; actual owner staging remains deterministic and zero-error but yields0/+1 net lots, loses23/19 painted cells and replaces one accepted seed1337 key. All10 accepted originals and4 differences are independently inspected. Review accepts rejection at **6.0 FAIL** and confines exhaustion to R8y's two common heuristic-positive straight cardinal links. See [rejection record](builds/democity_r9j_legal_alley_rejected.md) and [independent review](critic/democity_r9j.md).
- Democity r9k independently accepts a bounded whole-owner render-sensitivity diagnosis with no product change. The critic checks all77 adjacent-control comparisons. Under diagnostic uncapped/headless-on policy, conventional stable-only medians are Props17.320%, Roads15.859%, Terrain9.893%, Buildings9.156% and longer Services5.234%; masks are non-additive attribution and do not pass the sustained performance gate. R9l now profiles real Props components before any candidate. See [builder diagnosis](builds/democity_r9k_owner_sensitivity.md) and [independent audit](critic/democity_r9k.md).
- Democity r9l independently accepts only a bounded Props diagnosis and verifier fail-exit repair. All three aggregate records remain failed. Reflection-off LOD1 has3/3 stable local cycles at+12.925882% with -28 draws/-405,048 triangles; lamp ground-light pools center near zero, lenses fail timing and halo responses oppose. No product or score changes. R9m now separates actual selector and shadow state before any candidate. See [builder diagnosis](builds/democity_r9l_props_component_attribution.md) and [independent audit](critic/democity_r9l.md).
- Democity r9m independently accepts selector/submission diagnosis and no immediate edit. Settled census records interchange top-down0/2540/0 and aerial0/1939/0 with no LOD1 shadows; ordinary street has30/247/1037,221 active and119 submitted in one casting chunk. Its exact split is41,496 color +59,976 three-cascade shadow triangles; variable color timing and negative shadow median do not justify weakening foliage. Top-down timing remains open. See [builder diagnosis](builds/democity_r9m_lod1_pass_attribution.md) and [independent audit](critic/democity_r9m.md).
- Transit r5 phases one real scheduled showcase bus to dwell at the featured real stop at 22:00 and widens the stop camera. The complete local W5 review inspects 16 standard and 10 directed 0-error captures, and its public probe passes. The score correctly remains **6.8 FAIL**: the evidence gap is repaired, but bus art, shadow coverage, sparse context and the absent tram remain open. See [builder evidence](builds/transit_r5.md) and [local review](critic/transit_r5.md).
- Current all-mode smoke is [smoke_w4.png](../shots/smoke_w4.png): 28.7fps,562draws,3,361,455triangles,errors=0. The 3M / 50fps composed target remains failed.

The active repository is `/Users/martingrahn/Documents/SimBuild`. `Documents/ChatGPT/SimBuild` and the nested `SimBuild/` copy are not the active root.

## What is finished

Wave2 and wave2b integrations are complete. Current democity/transit r3 integration is applied and checked: native line creation/editing/cancellation, fleet/color controls, legacy save migration, explicit load errors/recovery, pause and real daily transit booking. Production build passes163modules. Nine viewed integrated captures have0errors. Ordinary-game isolated1080p High M4 Metal performance is23.40–30.58FPS, with921peak draws and6.92Mtriangles: the50FPS/3Mtargets still fail.

The all/democity startup defect is fixed. Both initialize the actual8,047resident/611building/30paid-facility city. This is deterministic native preroll with a231,000principal loan, not ordinary UI-earned progression. Pre-integration critic treasury25,848 becomes about25,797 in final noon captures with the transit finance adapter; no compensating cash was injected.

Current module verdicts: democity6.0/10 (API and exact cross-seed restage pass; broader gates fail), transit6.8/10 (API true). Both fail8.5. Services7.0 and infoviews6.4 remain their prior independent verdicts. All other scores and complete issues are in `STATUS.json`; no whole-game score exists and audio is unscored.

## Durable state

- Current report: [integration_w3.md](critic/integration_w3.md) and structured JSON beside it.
- Original independent reports: [democity_r3.md](critic/democity_r3.md), [transit_r3.md](critic/transit_r3.md).
- All status, full ranked module issues, failed clauses, next cursors and stop boundary: [STATUS.json](STATUS.json).
- Backups, exact hash checks, gameplay save and runtime recovery: [RECOVERY.md](RECOVERY.md).
- Current workflow records: `shots/workflows/w3/001` through `013`, plus `complete.json`; all cached requests/results must be preserved.
- Final source/documentation checkpoint: `/Volumes/ExtDrive/SimBuild-verification-2026-09-06/checkpoints/w3-r3-integration-2026-09-08-final`.

## W5 work order

6. Small owner-safe performance candidates are exhausted through r7e. Keep the accepted 84 m building LOD and full PBR/window material. A later draw-call redesign requires a separate bounded design proving dynamic rebuild, raycast, memory and exact-restage safety; do not infer it from visibility masks.
7. Cross-seed lifecycle equivalence is fixed through r7h. Keep terrain regeneration and roads new-world reset in their owner modules; ordinary deserialize retains monotonic IDs. Seed spatial variation still fails independently at 0/9 district centroids over 50 m.
6. The objective ordinary-play economy envelope passes through r7k. Continue with the highest-ranked verified visual/gameplay defect that can be resolved without weakening the passing geometry, memory, determinism, rollback or economy gates. Preserve traffic's explicit real-time independence from `?speed=`.
5. Night-sky separation is improved through r7l while wide-view light density remains failed. r7w shows scalar far-window changes up to25% do not move the threshold and other emissive owners contribute little; retain7% and wait for real frontage/source density. r7p materially reduces golden-hour clipping, but the upper-sky flat patch still fails. Further sky work needs plausible cloud/upper-sky structure; do not add metric noise, a competing global light or another composer.
6. Keep transit/traffic collision coordination, tram, ground seating, weak seed variation and subjective ordinary-play pacing as ranked open work. Successful-load Transit state and next-day finance are exact through r7x; whole-world failed-load rollback passes at r7j/r7x, objective30/90-day balance passes at r7k, and bridge clearance passes at r5p. No defect is considered fixed without fresh evidence.
7. The stale aerial autocorrelation failure is closed by r7q and the numeric noon activity clause by r7r. r7s exhausts the current bounded rank1 layout scalars without a safe scale pass. r7t/r7u/r7v provide measured terrain, props and road margins but still fail50fps. Services640m is visually rejected, road/service shadow masks do not help, and the remaining coarse owner thresholds are exhausted. Continue with the highest-ranked visual/gameplay issue that has a measurable owner-safe path; diagnose night wide-view source density before changing lighting.
8. r8g completes the bounded real-frontage diagnosis and extension14→32. It does not close rank1: just4,882/9,964 painted cells are claimed, valid stock stays612 and added owner respawns change forms/economy. Do not repeat r7s grid/no-split scalars or extend mixed-use flags again. Any later scale attempt needs coordinated zoning/building frontage evidence.
8. r8h partially improves rank2 with an unchanged LOD1 geometry budget. Do not repeat r8f's rejected48-card, opaque tetrahedral-lobe or camera-facing-normal trials, and do not claim top-down equivalence without a fresh pair. Distant planar impostors, exposed stems and repetitive hillside distribution remain.
9. r8i accepts only the stable near-window cue. Keep its baked state and atlas ownership, and do not claim rank3 closure: broad flat panes, service-yard ovals and fixture/facade/ground cohesion remain. r7w already proves that far-window emission floors12% and25% do not move the failed wide-view fraction; do not repeat scalar emissive trials, enlarge lamp coins or add another light/composer.
10. Rank4's post-road terrain-pad paths remain rejected: full pads hit5,337–5,986 road cells and support pads hit2,195–2,422. Preserve r8j/r8k static Northbank detail and its explicit non-operational limits, plus r8l/r8m/r8n's accepted environment contracts. R8o exhausts four local mountain-shading candidates and retains the restored material; broader geology work requires coordinated distribution/landform evidence. Preserve R8p's Traffic-owned scalar signal contract and R8q's hidden-only fallback-mast return. R8s exhausts the current LOD1 material hypothesis and retains accepted Standard foliage. Keep required headless readback.
11. Return to rank1 city fabric on accepted source. R8v closes only redundant membership, R8w closes the raw slot-key evidence gap, R8x rejects four global orders, and R8y finds no existing-node/cardinal link that passes its strict screen with positive estimated yield across both seeds. Preserve the proxy limits and do not promote a road without actual road-owner/frontage evidence. R8z rejects the one-whorl LOD1 branch candidate. R9a accepts only the bounded175→205m normal-camera range using the existing LOD1 crown. R9b accepts a qualified unchanged-source diagnosis and R9c repairs its evidence path: synchronized matrices and same-update copied-output membership close the forced520/1,775 partition mismatch, while the ordinary endpoint remains within the existing6m camera threshold. R9d confirms a large structural LOD1/impostor mismatch across angles and rejects alpha0.50. R9e accepts the bounded broad/narrow structural side-atlas change. R9f independently closes its measured142-pixel top-cell edge effect without a product change. R9g appends an actual16m street after accepted road IDs and rejects it: -1/0 net lots, lower claimed cells and broad fresh-world allocation changes. R9h independently traces the two real startup lot-generation calls and confines the frontage changes to1/2 existing edges. R9i independently rejects the narrower same-pair alley: it yields +1/+2 lots but fails the public player-tool grade contract at20.80%/12.32%. R9j pre-screens the only other common positive R8y link legally at9.73%/7.36%, then locally rejects actual owner yield0/+1 with painted-cell loss23/19. These exhaust only R8y's two common positive existing-node/cardinal candidates. Leave isolated link enumeration after R9j review and choose another measured ranked issue; a later scale attempt needs a broader coordinated or non-cardinal layout design. Keep range205m, transition band12m, CAP1=520 and update threshold6m unchanged. Treat histograms as fade-inclusive submitted counts, not unique visible trees. Preserve records, placement, material, atlas ownership, RNG and the normal CAP1 guard.

Whole-world owner failure now rolls back to the canonical pre-attempt state. If rollback itself meets a second owner refusal, core reports it and requires known-good recovery. Derived Transit demand and forecast can still recompute on a successful load; that continuity limit remains documented.

## Runtime

Dev server is the current terminal session at `http://127.0.0.1:5180`. Port 5173 is occupied by an unrelated Vite project. Use actual Chrome and `SIM_GL=metal`. References are `/Users/martingrahn/.simbuild/ref`. External evidence is `/Volumes/ExtDrive/SimBuild-verification-2026-09-06`; keep ExtDrive mounted. If a later authorized session must restart, follow RECOVERY first and smoke-test the existing source before development.

Historical, superseded continuation instructions are archived in `history/HANDOFF-before-w3-final-2026-09-08.md` and `history/RECOVERY-before-w3-final-2026-09-08.md`. They do not override the current execution control.

## Human playtest R11 live fixes — 2026-09-11

The current production source includes four locally accepted follow-ups with critic scores unchanged: small-city activity now produces a visible sparse fleet (22 people -> target/live 17) while an empty isolated road remains exactly zero and dead-end/portal contracts still pass; full-night exposure/environment fill now keeps terrain, shore and road placement readable in inspected 18:30/22:00 normal-entry captures; outside utility imports remain as backup until local facilities actually cover every current building; and Happiness can be exited by toggling its chip, Escape or Select / Inspect without breaking the later Escape -> Pause -> Save Game path. `tools/playtest-traffic-causality.mjs`, `tools/playtest-r10-ux-utilities.mjs`, the simulation selftest and the 163-module production build pass. Evidence is under `shots/playtest-fixes-r10` and `shots/playtest-fixes-r11`.

PT-20 remains partially open at the presentation layer: buildings need visible utility warning affordances and the service UI needs to explain road-borne distribution; no cable tool exists in the established architecture. Continue next with that bounded UX fix, then PT-16 using Zoning's real zonable/frontage map, followed by the real Roundabout defect (PT-17) and river attribution (PT-14). Preserve R10/R11 contracts, record failed experiments, and do not change critic scores without a new formal review.

## Human playtest R11 continued — 2026-09-12

PT-16, PT-17 and PT-22 are now locally fixed and verified with critic scores unchanged. The road tool exposes Zoning's authoritative buildable-frontage cells. The previously inert Roundabout card creates one atomic terrain-aware transaction containing eight real curved one-way Roads edges and eight snap nodes; the ring is a directed cycle, its center is not zonable, and exact undo/redo/save/load plus bulldoze-undo pass. The earlier sequential-arc implementation was rejected and removed because each intermediate terrain rebuild could invalidate later preview-approved arc grades. Fullscreen is now discoverable in both main and pause menus and its enter/exit browser contract passes. Evidence is `shots/playtest-fixes-r11/frontage-road-tool.*`, `roundabout-contract.json`, `roundabout-preview.png`, `roundabout-built.png` and `fullscreen-contract.json`.

The next measured product issue is PT-14, the human-reported river/sky-hole rendering defect. Attribute reflection, water depth/absorption, motion and bank contact independently before accepting a water change. PT-20's building utility warning and road-distribution explanation remains the next presentation task. Human retest is still required; no formal critic score changed.

## PT-14 river accepted locally — 2026-09-12

The human river/sky-hole defect is locally closed with scores unchanged. Terrain's existing sea mask now reduces broad reflection strength and minimum reflection mix only for inland water, revealing the established depth body; a restrained aerial flow cue remains. Matched valley baseline/candidate, open-sea coast, and normal-entry noon/night captures were inspected with errors=0. Terrain geometry, water collision, simulation state, draw count and triangle count are unchanged. See `docs/builds/playtest_r11_river.md`, `docs/critic/playtest_r11_river.md` and `shots/playtest-fixes-r11/river-*`. Human retest remains required. Continue with the PT-20 utility-warning/distribution presentation layer without altering service behavior.

## PT-20 utility presentation accepted locally — 2026-09-12

The final recorded PT-20 presentation gap is locally closed with scores unchanged. UI projects power/water shortage markers from Simulation's public per-building coverage records and caps them to the nearest 48 affected completed buildings. Electricity, water/sewage and garbage panels explain connected-road distribution, outside imports and the absence of separate cable/pipe tools. No Economy, Services, Roads, coverage, import or save behavior changed. The directed marker/guide probe, existing import-replacement probe, fullscreen regression and normal startup/save/load probe pass with errors=0. See `docs/builds/playtest_r11_utility_warnings.md`, `docs/critic/playtest_r11_utility_warnings.md` and `shots/playtest-fixes-r11/utility-*`. Human retest remains required. The next verified work is cold-start profiling, since feedback is fixed but underlying startup time remains open.

## Cold-start attribution after R11 fixes — 2026-09-12

A fresh normal-entry profile still reaches readiness in about 3.1 seconds. The measured module costs are Terrain 1,863ms, Props 397ms, Audio 289ms, Environment 35ms and UI 3ms. The honest loading shell remains immediate. No quality-reducing or ownership-changing startup candidate was accepted; the next autonomous step is internal Terrain-stage attribution before any product edit. The complete recorded human-finding fix set now needs subjective player retest.

## GameVolt cloud saves — 2026-09-12

SimBuild now uses GameVolt's existing Supabase-backed SDK during normal play on `gamevolt.io`. Guests keep the atomic IndexedDB slot system. Signed-in players synchronize Autosave and Slots 1–3 across devices; each slot resolves by `savedAt`, deletions use durable tombstones, and switching accounts mirrors the newly signed-in account instead of uploading the previous account's local cities. Cloud payloads use browser-native gzip/base64 with backward-compatible raw JSON reads. The measured current city save is about 3.4 MB locally and 2.0 MB encoded.

The production Supabase `games` registry contains `simbuild`, and its reproducible seed statement is in GameVolt `sql/simbuild.sql`. The pure merge/compression contract probe, a real IndexedDB replacement/delete probe, a mocked signed-in save/delete round trip and the production build all pass with zero page errors. Gameplay serialization, restore ownership and critic scores are unchanged. Human cross-device login/reload remains the final subjective verification.

## Main-menu cloud-save invitation and demo city — 2026-09-12

The player-facing main menu now explains the cloud-save benefit in plain language and offers a direct GameVolt sign-in button when the hosted SDK is available. Signed-in players instead see confirmation that their city can be continued on another computer. The invitation is optional and does not block play. The empty-terrain menu backdrop has been replaced with a 332 KB WebP capture of the real deterministic Democity at 8,073 residents; the image was captured through the normal rendering path and the menu labels it as a demo city. Normal New Game still starts the unchanged empty-map play state.

Local anonymous and mocked signed-in menu contracts, the `/simbuild/` production build, and a 1440×900 visual capture pass with zero simulation/page errors. Evidence is `shots/cloud-signin-menu/democity-aerial.*` and `shots/cloud-signin-menu/main-menu-promo.png`. Critic scores and gameplay contracts are unchanged.

## City-slot lifecycle — 2026-09-12

The main menu now follows the conventional city-save flow. Continue loads the active city slot, falling back to the most recently saved city, and is disabled when no city exists. New Game requires Slot 1–3, identifies occupied slots by city name/day, and requires a second explicit confirmation before replacement. A newly created city is saved atomically to its chosen slot before entering play, and daily autosave updates that active city instead of overwriting one shared autosave across several cities. Older slot metadata is upgraded from its intact save payload so city names appear without rewriting gameplay state.

Load Game is now labelled Load / Manage Cities and exposes city names, slot/day/age, Load, and a two-step Delete city action. Deletion keeps the established cloud tombstone behavior. An end-to-end browser contract verified empty Continue, new-city Slot 1 creation, direct entry into play, active Slot 2 Continue, exact restore, confirmed deletion, cloud tombstone retention, and occupied-slot replacement confirmation at 1280×720 with zero errors. Evidence is `shots/save-flow/new-game-slot.png` and `shots/save-flow/manage-and-confirm.png`. Serialization ownership, simulation and critic scores are unchanged.

## R13 close vehicle art — 2026-09-12

PT-26 is partially repaired with scores unchanged. The rigid box truck is no longer one continuous inflated body: Traffic authors separate cab, cargo box and chassis volumes with mirrors, grille, lamps, plate, rear-door framing and side reflectors while retaining exact dimensions and simulation ownership. Cargo panels preserve more deterministic paint variation. The inspected matched fleet images are zero-error and the peak changes 875,398 -> 874,678 triangles at the same 112 draws. The full traffic causality/determinism/restore probe and production build pass. See `docs/builds/playtest_r13_vehicle_art.md`, `docs/critic/playtest_r13_vehicle_art_local.md` and `shots/playtest-fixes-r13/`. Traffic remains **7.2 FAIL** and whole-game **6.0 FAIL**; broader fleet art and ordinary-play human replay remain open.

## R14 building continuity and economy clarity — 2026-09-12

The repeated whole-chunk building blink is locally fixed: a dirty chunk now receives its camera LOD in the rebuild frame, rather than waiting up to 200ms for the periodic sweep. A forced real building update kept exactly one chunk LOD visible for all16 observed frames with errors=0. Zoning now explains live demand and why low-demand painted land waits. Statistics exposes the existing Simulation income/expense terms and identifies the largest cost during a deficit.

The non-working Hamlet loan was a UI/contract mismatch: the button requested ¢50,000 against a real ¢30,000 milestone capacity, then displayed an optimistic message for the rejected action. It now offers the available amount, reports approval/refusal immediately, and displays debt, daily payment and credit. Browser verification records ¢150,000→¢180,000, ¢32,400 debt, ¢1,080/day, truthful rejection at zero credit and exact loan save/load. Build and Simulation determinism pass. See `docs/builds/playtest_r14_buildings_economy.md`, `docs/critic/playtest_r14_local.md` and `shots/playtest-fixes-r14/`. Scores remain **Buildings 7.4 FAIL**, **UI 7.0 FAIL**, **Simulation 6.5 FAIL**, and **whole-game 6.0 FAIL**. Human replay and a formal bankruptcy/recovery loop remain open.

## R15 local save recovery and prior-release compatibility — 2026-09-12

Each IndexedDB city slot now retains its preceding valid payload atomically with the newest save. If the newest JSON or an owner restore fails, loading attempts the local recovery copy, retains the original error for diagnostics and tells the player that the previous safe city opened. Cloud-to-local replacement preserves this local recovery copy, and malformed current JSON is never promoted over it. Props revision and Traffic pedestrian/pose fields now reserialize exactly after restore.

The deliberate corruption probe recovers treasury150,000 from a broken151,111 current save, shows the warning, and leaves valid primary/recovery copies after saving again. A populated save produced by the actual published R13 commit `08b1a2d` restores under R15 with all14 module payloads exact:604 roads,9,964 cells,612 buildings,31 services and population8,025. Forced late Transit rejection restores every owner, terrain, time and camera exactly. Build, cloud bundle and Simulation determinism pass. See `docs/builds/playtest_r15_save_recovery.md`, `docs/critic/playtest_r15_local.md` and `shots/playtest-fixes-r15/`.

The player's original hosted Slot1 payload was unavailable to automation, so that exact error remains unreproduced. The prior revision is local and is not copied as a second multi-megabyte cloud revision to a new computer. Cross-device cloud history remains open. Scores stay unchanged: **Props6.4**, **Traffic7.2**, **UI7.0**, **Simulation6.5**, **whole-game6.0**, all FAIL.
