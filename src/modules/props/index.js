// STUB — replaced by the props builder. Must init without errors.
export default {
  name: 'props',
  dependencies: [],
  budget: { drawCalls: 10, triangles: 10000 },
  async init(ctx) { ctx.log.info('stub init'); },
  update(dt, ctx) {},
  dispose(ctx) {},
  api: {},
  showcase: {
    description: 'props stub — nothing staged yet',
    async setup(ctx) {},
  },
};
